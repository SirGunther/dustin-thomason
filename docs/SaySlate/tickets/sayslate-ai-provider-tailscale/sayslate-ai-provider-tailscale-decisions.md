# SaySlate AI Provider and Tailscale Integration — Decisions

Requirements: [sayslate-ai-provider-tailscale-requirements.md](./sayslate-ai-provider-tailscale-requirements.md)

| ID | Decision | Why | Serves | Source | Supersedes or rejects |
| --- | --- | --- | --- | --- | --- |
| LD-001 | The connection control must support OpenAI, Anthropic Claude, Gemini, and a custom provider. | These are the provider families explicitly requested. | REQ-001, REQ-002, REQ-004 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), paragraph 2 | Gemini-only configuration as the final feature |
| LD-002 | The first custom-provider target is LM Studio reached over Tailscale. | This is the explicit private-endpoint use case. | REQ-005, REQ-006 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), paragraphs 1–2 | Public exposure as the primary path |
| LD-003 | Requests use JSON Schema output whenever the selected provider/model supports it. | The user explicitly requested schema output where possible rather than as an unconditional provider assumption. | REQ-007 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), rough MVP | Unconditional JSON Schema support |
| LD-004 | Argus is read-only reference material for provider and LM Studio integration. | The user named its working integration as the reference implementation. | REQ-008 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), paragraph 1 | Making SaySlate depend on Argus runtime code |
| LD-005 | Provider configurations must persist independently rather than one save replacing another provider. | The user explicitly identified provider replacement as unexpected behavior. | REQ-002, REQ-003 | Dustin Thomason, 2026-09-22 clarification | A single destructive active-provider record |
| LD-006 | One high-reasoning orchestrator owns architecture and oversight; low-reasoning agents receive bounded implementation tickets. | The workflow is intended to prevent low-tier agents from improvising architecture. | REQ-009 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), orchestration paragraphs | Tierless unbounded implementation |
| LD-007 | Subagent completion is not an orchestration pause point. | The user explicitly rejected the earlier pause behavior. | REQ-010 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), “ignore the pause portion” | Pausing after every subagent return |
| LD-008 | Checklists remain visible in chat, while durable implementation and review evidence lives in the artifact set. | The checklist is mandatory for visibility; evidence must survive chat. | REQ-011 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md); Dustin Thomason, 2026-09-22 clarification | Evidence only in chat |
| LD-009 | Every substantive completion or blocker sends the standard completion notification. | The user explicitly required the notification rule. | REQ-012 | [Original ticket](./sayslate-ai-provider-tailscale-original-ticket.md) | Silent completion |
| LD-010 | The work uses the `agentic-handoff` multi-file set nested under one ticket folder. | The user is testing this structure in place of the monolithic TODO. | REQ-013 | Dustin Thomason, 2026-09-22 clarification invoking `agentic-handoff` | The single-file TODO as the active orchestration contract |
| LD-011 | Objectives and findings use `State`, `Value`, `Evidence`, and `Depends on`. | The user identified the PRDV-16936 compact record as the correct reporting format. | REQ-014 | Dustin Thomason, 2026-09-22 clarification citing `PRDV-16936-CASE-DETAIL-PROTOTYPE-TODO.md` | Generic narrative evidence ledgers |
| LD-012 | Production-path evidence is required; a passing test alone does not establish correctness. | Tests must represent the real work failure point, not substitute for it. | REQ-015 | Dustin Thomason, 2026-09-22 clarification | Test-only correctness claims |
| LD-013 | `SirGunther/SaySlate` is the canonical implementation repository and `C:\SaySlate` is its local working location; the original PDProjects folder is preserved read-only. | The user explicitly selected the GitHub repository and requested a C-drive worktree beside Argus and WhisperService. | Process decision (`Serves: —`) | Dustin Thomason, 2026-09-22 clarification; commit `af9a2f3a8cbad88c22edc094767b5cdd31f1a24e` | Treating the original OneDrive folder as the writable implementation location; superseded in part by LD-020, which removes that copy from the active source boundary |
| LD-014 | Provider profiles, including endpoints, model IDs, and API keys, persist in `chrome.storage.local` within the installed browser profile; switching providers preserves every configured profile. The data is not synced between installations, and no native credential host is introduced. | The user wants configuration to survive provider switching in one extension installation without adding a separate credential system. | REQ-002, REQ-003 | Dustin Thomason, 2026-09-22 clarification | Session-only re-entry, synchronized credentials, a native credential host, and a single destructive active-provider record |
| LD-015 | Gemini keeps the existing native Gemini adapter. It moves to an OpenAI-compatible transport only if a demonstrated incompatibility makes that necessary. | The current adapter works, so changing it without a real failure would add risk without user value. | REQ-001, REQ-007 | Dustin Thomason, 2026-09-22 clarification: “if it ain't broke, don't fix it” | Replacing the working Gemini adapter merely to unify transports |
| LD-016 | AI inference remains owned by the initiating full-page or floating SaySlate renderer. Closing, reloading, or navigating away may cancel the request; SaySlate preserves the source text and last completed result. | This matches the working Gemini path, supports slow calls while the surface remains alive, and avoids inventing a durable service-worker/offscreen owner without demonstrated need. | REQ-001, REQ-007 | Dustin Thomason, 2026-09-22 confirmation of decision 3; EV-017 | Detached inference completion as an MVP requirement |
| LD-017 | Provider setup offers named presets with their known default endpoint plus an editable endpoint field. Users can save provider-specific endpoints, models, and keys, and can create/test a custom endpoint. Custom HTTPS origins use optional host permission requested for the exact configured origin when it is saved or tested. | Presets streamline common providers while the editable field preserves custom Tailscale/LM Studio use; per-origin permission follows least privilege without blocking arbitrary secure endpoints. | REQ-001–REQ-006 | Dustin Thomason, 2026-09-22 clarification of decision 4; EV-012 and EV-018 | One fixed endpoint per provider and broad install-time custom-host access |
| LD-018 | Test Connection follows Argus's non-generative pattern: validate configuration, reachability, authentication, and selected-model discovery through provider-specific health/listing operations; send no user transcript and perform no billable inference. Show the specific result in the provider settings surface and a transient success/failure notification. | This provides useful diagnostics without consuming generation quota or exposing user content. Structured-output behavior remains governed and validated during actual inference. | REQ-004, REQ-007, REQ-008 | Dustin Thomason, 2026-09-22 direction to use Argus as the reference; `C:\Argus\runtime\desktop-application.mjs:375-385,1709-1737`; `C:\Argus\app.js:781-803` | Treating model-list success as proof of structured inference, or spending inference tokens merely to test configuration |
| LD-019 | Delivery proceeds sequentially from shared foundations to live proof: provider-profile schema and migration; request/permission and adapter boundaries; provider transports; connection testing; provider-selection UI integration; then live Tailscale/LM Studio acceptance. Tickets sharing settings, manifest, or routing ownership do not run concurrently. | This order stabilizes shared state and permissions before adapters and UI consume them, then reserves live acceptance for the integrated path. | REQ-001–REQ-016 | Dustin Thomason, 2026-09-22 delegated ticket-order authority to the orchestrator; dependency analysis in decision 6 | User-authored ticket sequencing and overlapping parallel ownership |
| LD-020 | `SirGunther/SaySlate` and `C:\SaySlate` are the only active implementation sources of truth. The original OneDrive copy is outside the active source and evidence boundary and may be deleted later by the user after validation. | Continuing to consult the imported copy creates ambiguity after the canonical repository is established. | Process decision (`Serves: —`) | Dustin Thomason, 2026-09-22 clarification of decision 7 | LD-013's treatment of the OneDrive copy as continuing read-only import evidence |
| LD-021 | Handoff generation is one continuous high-reasoning authoring process covering foundation documents, decomposition, ticket files, and the handoff. Tiering begins only after the completed set reaches a high-reasoning orchestrator, which dispatches the bounded tickets to lower-reasoning implementation agents. | The user deliberately kept authoring on extra-high reasoning because decomposition requires architectural understanding; low-reasoning models are appropriate only after ticket boundaries are complete. | REQ-009, REQ-010, REQ-016 | Dustin Thomason, 2026-09-22 clarification | Treating ticket creation as a low-reasoning stage or pausing authoring for a model switch |
| LD-022 | Provider adapters request one canonical structured result, `{ "text": string }`, whenever the selected provider/model supports schema enforcement; each adapter validates and unwraps that result to the existing SaySlate string boundary. | A tiny shared result preserves existing first/second-pass consumers, gives every provider the same validation target, and avoids exposing provider response shapes to UI code. | REQ-001, REQ-007 | EV-003–EV-005 and EV-020–EV-022; high-reasoning decomposition delegated by Dustin Thomason on 2026-09-22 | Returning provider-native response objects to the renderers or inventing different output shapes per provider |
| LD-023 | Provider state uses storage key `sayslate-ai-provider-profiles` and version-1 shape `{ version, activeProfileId, profiles }`; each profile is `{ id, name, providerKind, endpoint, modelId, credential }`. `globalThis.SaySlateAIProviderSettings` exposes `load`, `upsertProfile`, `activateProfile`, `deleteProfile`, `clearCredential`, and `migrateLegacyConfig`. Unsupported or malformed stored versions fail without overwriting storage. `upsertProfile` takes credential action `retain`, `replace`, or `clear`, and deleting the active profile leaves no active profile. | The exact record and callable boundary prevent storage, permission, and UI tickets from inventing incompatible profile shapes or destructive credential behavior. | REQ-002, REQ-003 | Dustin Thomason, 2026-09-22 direction that high-reasoning authoring must complete the implementation decomposition; EV-002, EV-025 | A single combined provider record, implicit blank-key deletion, and silent reset of malformed storage |
| LD-024 | `globalThis.SaySlateAIProviderRegistry` recognizes provider kinds `gemini`, `openai`, `anthropic`, and `custom`; transport kinds are `gemini-native`, `openai-chat-completions`, and `anthropic-messages`. Preset base URLs are editable starting values (`https://generativelanguage.googleapis.com/v1beta`, `https://api.openai.com/v1`, `https://api.anthropic.com/v1`, and blank custom). Endpoint normalization accepts absolute HTTPS URLs, removes a trailing slash, and rejects embedded credentials, query strings, fragments, non-HTTPS schemes, and invalid origins. Discovery strategies are Gemini exact-model retrieval and exact-ID matching in OpenAI-compatible or Anthropic model lists. The registry exposes preset lookup, endpoint normalization, discovery strategy, and exact origin-pattern derivation. `globalThis.SaySlateAIProviderPermissions.ensureForEndpoint` returns `{ ok, code, originPattern }`. | The registry fixes every value consumed by transport, connection-test, and UI tickets while keeping user endpoints editable and permissions least-privilege. | REQ-001, REQ-002, REQ-004, REQ-005 | Dustin Thomason, 2026-09-22 provider-preset/editable-endpoint direction and delegated high-reasoning decomposition; EV-018, EV-020–EV-023 | URL-shape inference in UI code, fixed custom endpoints, and broad required host permissions |
| LD-025 | `globalThis.SaySlateAIProviderClient.generate({ profile, systemPrompt, userPrompt, fetchImpl?, timeoutMs?, signal? })` is the only renderer-facing generation contract and resolves to a plain string. Provider adapters validate LD-022's schema before returning and never silently fall back to unvalidated free-form output. Failures are `Error` objects with bounded `code` values: `invalid_configuration`, `authentication_failed`, `model_unavailable`, `structured_output_unsupported`, `malformed_response`, `request_timeout`, `network_error`, or `provider_error`; messages contain no credential, prompt, transcript, or raw response body. | One dispatcher contract preserves the existing renderer call shape while preventing provider envelopes and sensitive diagnostics from leaking into UI code. | REQ-001, REQ-006, REQ-007 | Dustin Thomason, 2026-09-22 direction that high-reasoning authoring must complete the implementation decomposition; LD-022; EV-003–EV-005, EV-017, EV-020–EV-024 | Provider-native response objects in renderers, free-form unvalidated fallback, and unbounded error forwarding; superseded in part by LD-031 |
| LD-026 | `globalThis.SaySlateAIProviderConnectionTest.test({ profile, fetchImpl?, timeoutMs? })` returns `{ ok, code, message, modelId }`, defaults to a 15-second timeout, and performs only LD-024's provider-specific model discovery. Its codes are `available`, `invalid_configuration`, `permission_denied`, `network_error`, `authentication_failed`, `model_unavailable`, `provider_error`, `malformed_response`, or `request_timeout`; the message is bounded and contains no secret or raw body. | The exact diagnostic contract lets the UI present useful Argus-style outcomes without performing inference or interpreting provider responses itself. | REQ-004, REQ-008 | Dustin Thomason, 2026-09-22 direction to follow Argus connection-test behavior and delegated high-reasoning decomposition; EV-019–EV-023 | A boolean-only result, inference-based testing, and raw provider errors in the UI |
| LD-027 | The existing full-page connection-icon surface owns provider management: saved-profile selection, provider preset, editable endpoint, model ID, masked credential entry, Save/Activate, Test Connection, Clear Credential, and Delete. A blank credential edit retains the saved value; only Clear Credential removes it; a stored credential is never rehydrated into or revealed by the input. The floating surface reads the active profile but does not manage profiles. | This is the smallest UI that supports persistent switching without exposing stored keys or duplicating settings management across surfaces. | REQ-001–REQ-004 | Dustin Thomason, 2026-09-22 endpoint/dropdown and persistence direction plus delegated high-reasoning decomposition; EV-002–EV-004, EV-029 | One destructive settings form, silent credential clearing, stored-secret reveal, and duplicate floating-profile management |
| LD-028 | New implementation remains dependency-free, uses classic scripts that expose `globalThis` modules, loads producers before consumers in both HTML surfaces, and adds focused `tests/*.test.mjs` files discovered by `tests/verify.mjs`. | This preserves the repository's existing buildless module and test conventions and avoids introducing package infrastructure for small fetch/storage adapters. | Process decision (`Serves: —`) | Dustin Thomason, 2026-09-22 “if it ain't broke, don't fix it” direction and delegated high-reasoning decomposition; EV-025–EV-028 | Adding a package manager, SDK dependency, bundler, or ES-module migration for this feature |
| LD-029 | Live acceptance is recorded in `docs/review/ai-provider-tailscale-validation-review.md`; only after successful proof may the acceptance ticket update feature-specific text in `README.md`, `[Unreleased]` in `CHANGELOG.md`, and the relevant `ROADMAP.md` entry. It does not choose or bump a release version. | A version-neutral acceptance record avoids inventing a release number while keeping proven capability and planning surfaces accurate. | REQ-005, REQ-015 | Dustin Thomason, 2026-09-22 evidence-first handoff direction and delegated high-reasoning decomposition; EV-030 | A speculative versioned review, version bump, or capability claim before live proof |
| LD-030 | `migrateLegacyConfig` moves the legacy Gemini key and model rather than copying them: in one `chrome.storage.local.set` it writes the new Gemini profile and rewrites `sayslate-grammar-config` without `apiKey` and `model`, leaving every prompt field unchanged. A legacy record with neither field is already migrated, so a deleted or credential-cleared profile is never recreated and no second copy of the key survives. | A copy-only idempotent migration would re-create a profile the user deleted and would leave a key behind after Clear Credential, contradicting LD-027. | REQ-003 | Open decision 8, answered by LD-027 and REQ-003; mechanism under the decomposition delegation cited by LD-023 | Copy-only migration that leaves the legacy key in place; amends LD-023's `migrateLegacyConfig` semantics |
| LD-031 | When a provider answers a schema-constrained generation request with HTTP 400 or 422, the adapter retries exactly once without the schema and accepts only a trimmed, non-empty text reply. The retry's outcome is final; an empty reply is `malformed_response`. Schema responses are still validated per LD-022 and LD-025. | Keeps the origin's "JSON schema whenever possible" and LD-003 intact while keeping models that cannot enforce the schema usable. | REQ-007 | Dustin Thomason, 2026-09-22, answer to open decision 9 | LD-025's rejection of every free-form fallback, for schema-rejection responses only |
| LD-032 | SAYAI-06's live Tailscale/LM Studio acceptance is deferred until the private endpoint exists, and may become a later configuration step. This session delivers SAYAI-01 through SAYAI-05 and does not dispatch SAYAI-06. | The endpoint does not exist yet, so live acceptance cannot run; everything else can proceed. | REQ-005, REQ-006 | Dustin Thomason, 2026-09-22: "that can be waived until later implementation" | LD-019's live-acceptance wave, for this session only |
| LD-039 | The Gemini native adapter also runs LD-031's single schema-free retry after an HTTP 5xx on the schema request, after a 1-second pause. Every Gemini failure message carries the HTTP status and Google's own error text, bounded to 200 characters with the credential redacted, and both renderers show that detail for `provider_error`. | A real first pass got HTTP 503 on the schema request, which had no recovery path; the generic message hid Google's reason, which the original client always showed. | REQ-007 | Dustin Thomason, 2026-09-23: observed HTTP 503 from `generateContent`; 2026-09-22 direction that retry policies are expected; SaySlate commit `cf4bc5d9b14bdd45fd5e36e0ab7910a73f506063` | LD-031's 400/422-only trigger and the fixed `provider_error` text, for the Gemini adapter only; retry portion superseded by LD-040 |
| LD-040 | LD-039's 5xx retry is removed. A Gemini 5xx fails on its first response, carrying the HTTP status and Google's own error text (bounded, credential redacted) as LD-039 set out; LD-031's 400/422 retry is unchanged. | The 5xx retry was added on an unconfirmed overload assumption, and in real use it left the pass hanging. | REQ-007 | Dustin Thomason, 2026-09-23: "The retry just locks up the system"; SaySlate commit `0998f485557b4140cc190722e9ccdf863b0dda97` | LD-039's 5xx retry and pause |
| LD-041 | Custom (LM Studio) profiles send `reasoning_effort: "none"` on the structured request. The 400/422 plain retry omits it, and OpenAI, Gemini, and Anthropic profiles never send it. | The user does not want a reasoning pass, and the request field overrides each machine's saved setting (EV-035). OpenAI rejects `reasoning_effort` on non-reasoning models, so it stays custom-only. | REQ-005 | Dustin Thomason, 2026-09-23: "I don't want to use thinking" and approval of the payload update; EV-035; SaySlate commit `2bafd4e31f51080edd087eff5895363923e0c49d` | Relying on each LM Studio machine's saved Enable Thinking setting |
| LD-033 | `migrateLegacyConfig` creates the Gemini profile with LD-024's Gemini preset endpoint `https://generativelanguage.googleapis.com/v1beta` and, when no profile is active, sets `activeProfileId` to that profile in the same single `chrome.storage.local.set` call. | The legacy record is the configuration the user runs today (EV-002–EV-004) against that exact base (EV-005's `ENDPOINT_ROOT`). SAYAI-05 must let the user run the same Gemini path without re-entering anything. A blank endpoint fails LD-026's pre-network configuration check, and an inactive profile forces re-selection. | REQ-003 | Orchestrator question-admission gate during the SAYAI-01 review, 2026-09-22. Answered by EV-002–EV-005, LD-024, LD-030, and SAYAI-05's migration checklist item; no user input required | A migrated profile with a blank endpoint or no active selection; amends the contents of LD-030's single write |
| LD-034 | `globalThis.SaySlateAIProviderPermissions` exposes two operations, both resolving to LD-024's `{ ok, code, originPattern }`. `ensureForEndpoint(endpoint)` is called only from a user-triggered Save or Test. It normalizes the endpoint through the registry and calls `chrome.permissions.request` for the exact origin pattern only when `chrome.permissions.contains` is false. `hasForEndpoint(endpoint)` never requests. Codes: `already_granted` and `granted` (`ok: true`), `permission_denied` (not granted, the user declined, or the request API rejected), and `invalid_configuration` (the endpoint fails normalization; `originPattern: null`). Neither operation throws or broadens the origin. | SAYAI-04 must fail before network access when permission is missing, without prompting. SAYAI-05 must request only on Save or Test. A fixed code set shared with LD-026's `permission_denied` and `invalid_configuration` lets both consumers map outcomes without interpreting Chrome errors. | REQ-004, REQ-005 | Orchestrator question-admission gate before the SAYAI-02 dispatch, 2026-09-22. Answered by LD-017, LD-024, LD-026, the SAYAI-02 checklist ("Do not request permission while loading…"), and the SAYAI-04 checklist ("Fail before network access when … host permission is missing"); no user input required | Undefined `ensureForEndpoint` codes, a request-only permission seam, and consumers calling `chrome.permissions` directly |
| LD-035 | Request details the transport adapters must follow. (1) Renderers keep their existing built pass prompt unchanged and pass it as `userPrompt`, omitting `systemPrompt`; every adapter omits its system field or message when `systemPrompt` is empty. (2) The Anthropic Messages adapter POSTs `<endpoint>/messages` with headers `x-api-key`, `anthropic-version: 2023-06-01`, and `anthropic-dangerous-direct-browser-access: true`. Its body has `max_tokens: 16000` and `output_config.format` `json_schema`, with no sampling parameters. `stop_reason` `refusal` maps to `provider_error`; `max_tokens` maps to `malformed_response`. (3) The OpenAI-compatible adapter POSTs `<endpoint>/chat/completions` with `Authorization: Bearer <credential>` only when the credential is non-empty, and `response_format: { type: "json_schema", json_schema: { name, strict: true, schema } }`, with no sampling or token-limit parameters. (4) The Gemini native adapter keeps today's key-in-URL request and `temperature: 0.1` (LD-015), adding only its native JSON-schema generation config. | SAYAI-03 must preserve prompt wording, and today's call sites pass one built prompt (EV-003, EV-004). EV-033 shows that sampling parameters are rejected by current Claude models and that API keys use `x-api-key`. Reasoning-model OpenAI endpoints also commonly reject non-default sampling, so omitting sampling keeps every non-Gemini model usable. LM Studio may run without auth (EV-014). | REQ-001, REQ-006, REQ-007 | Orchestrator question-admission gate before the SAYAI-03 dispatch, 2026-09-22. Answered by EV-003, EV-004, EV-014, EV-020, EV-033, LD-015, LD-022, and SAYAI-03's "Must not change … prompt wording"; no user input required | Bearer API-key auth to Anthropic, sampling parameters on non-Gemini adapters, and splitting or rewording prompts |
| LD-036 | (1) The Gemini native adapter sends LD-022's schema as `generationConfig.responseJsonSchema`, with `responseMimeType: "application/json"`, never as `responseSchema`. (2) A Gemini HTTP 400 whose `error.details[]` contains `reason: "API_KEY_INVALID"` is `authentication_failed`. It is not a schema rejection, so LD-031's retry does not run. (3) An OpenAI-compatible `choices[0].message.refusal` maps to `provider_error`, as LD-035(2) maps Anthropic refusals. | Under EV-034, `responseSchema` rejects the canonical schema's `additionalProperties`. Every Gemini request would then fall back through LD-031 to unvalidated free text and cost a second call, which defeats LD-003's "whenever possible". EV-034 also shows Gemini reports a bad key as 400, so without (2) a bad key costs a pointless retry and is misreported as `provider_error`. (3) applies LD-035's existing refusal mapping to the one transport it did not name. | REQ-004, REQ-007 | Orchestrator question-admission gate during the SAYAI-03 review, 2026-09-22. Answered by EV-034, LD-003, LD-022, LD-025, LD-031, and LD-035; no user input required | Amends LD-035(4)'s Gemini schema field; refines LD-031 for Gemini authentication failures; extends LD-035(2)'s refusal mapping to OpenAI-compatible transports |
| LD-037 | `SaySlateAIProviderConnectionTest.test` runs its pre-network checks in order: registry `normalizeEndpoint` (`invalid_configuration`); model ID present (`invalid_configuration`); credential present for `gemini`, `openai`, and `anthropic` (optional for `custom`, per EV-014; `invalid_configuration`); then `SaySlateAIProviderPermissions.hasForEndpoint`, whose `permission_denied` or `invalid_configuration` passes through unchanged and which never requests. Discovery requests: Gemini `GET <endpoint>/models/<modelId>?key=<credential>` (LD-015's key-in-URL form); OpenAI-compatible `GET <endpoint>/models`, with `Authorization: Bearer` only when the credential is non-empty; Anthropic `GET <endpoint>/models?limit=1000`, with LD-035(2)'s three headers, following `after_id=<last_id>` while `has_more` is true, capped at 10 pages. Mapping: 2xx with the exact ID found → `available`; a Gemini 2xx whose `name` is `models/<modelId>` → `available`; a valid list without the ID, or Gemini 404 → `model_unavailable`; 401, 403, or Gemini 400 `API_KEY_INVALID` → `authentication_failed`; fetch rejection → `network_error`; abort at the 15-second default → `request_timeout`; an unparseable or wrong-shape 2xx body → `malformed_response`; any other status → `provider_error`. | SAYAI-04 must fail before network access on missing configuration, credential, or permission, and must distinguish reachability, authentication, permission, and selected-model outcomes without interpreting Chrome or provider errors in the UI. These values reuse the request shapes, header sets, pagination, and invalid-key envelope that EV-020, EV-033, EV-034, LD-034, LD-035, and LD-036 already establish. | REQ-004, REQ-008 | Orchestrator question-admission gate before the SAYAI-04 dispatch, 2026-09-22. Answered by EV-014, EV-020, EV-033, EV-034, LD-015, LD-024, LD-026, LD-034, LD-035, and LD-036; no user input required | Credential-less OpenAI/Gemini/Anthropic tests, single-page Anthropic listing, and UI-side interpretation of discovery responses |
| LD-038 | Renderer integration semantics for SAYAI-05. (1) Both surfaces run `SaySlateAIProviderSettings.load()` and then `migrateLegacyConfig()` at startup, before reading prompts. From then on, no renderer reads, defaults, or writes `apiKey` or `model` in `sayslate-grammar-config`; that record carries only prompt fields, `promptSchemaVersion`, and `secondPassEnabled`. (2) Each AI pass on either surface resolves the active profile from storage at pass start. No cached profile is used for a request, and a missing active profile shows a notice to choose a provider in connection settings. (3) Selecting a saved profile in the full-page control calls `activateProfile` and loads that profile's saved endpoint and model into the form; the credential input stays blank. Save upserts the form into the selected profile, or a new one, and then activates it. Its credential action is `replace` when the credential input is non-empty and `retain` otherwise. Clear Credential calls `clearCredential`. Delete calls `deleteProfile`. (4) The Save and Test click handlers call `SaySlateAIProviderPermissions.ensureForEndpoint` as their first awaited operation. A result that is not `ok` shows its code inline and writes nothing. (5) Save stores `normalizeEndpoint` output; an invalid endpoint blocks Save inline. Test runs `SaySlateAIProviderConnectionTest.test` on the form's current values, using the saved credential when the credential input is blank, and never writes storage. (6) The profile `name` is derived at Save as `<label> · <modelId>`, with labels Gemini, OpenAI, Claude, and Custom; there is no name input. A new Gemini form starts with model `gemini-3.1-flash-lite`, the existing default; other presets start empty. | (1) Without it, `app.js`'s `normalizeProcessingConfig` default model and whole-object writes (`app.js:183,267,342`) write `model` back into the legacy record after migration. `migrateLegacyConfig` would then create a new keyless Gemini profile on every load, violating LD-030 and REQ-003. Floating-first users also need migration because `floating.js` reads the same record (EV-004). This supersedes the SAYAI-01 audit note that only the full page migrates; the residual risk is a duplicate Gemini profile if both surfaces first-load at the same instant, with no key loss. (2) applies SAYAI-01 F4's read-before-write principle to reads that drive requests. (3) implements REQ-003's "saving or selecting" and LD-027's control list. (4) follows the SAYAI-02 audit's transient-activation constraint. (5) and (6) fill LD-027's list without adding controls. | REQ-001–REQ-004 | Orchestrator question-admission gate before the SAYAI-05 dispatch, 2026-09-23. Answered by REQ-003, EV-004, EV-029, LD-023, LD-024, LD-026, LD-027, LD-030, LD-034, the SAYAI-01 and SAYAI-02 audits, and `app.js:183,252-273,342`; no user input required | Full-page-only migration (the SAYAI-01 audit note), renderer-held `apiKey`/`model` fields, cached profiles for requests, and a separate profile-name control |

## Open Decision Checklist

- [x] 1. May provider profiles persist in local extension storage without a native credential host? — Resolved as LD-014
- [x] 2. Should Gemini retain its native adapter? — Resolved as LD-015
- [x] 3. Must in-flight inference survive its initiating SaySlate surface closing, reloading, or navigating? — Resolved as LD-016
- [x] 4. Should custom-provider origins use install-time or individually requested host permissions? — Resolved as LD-017
- [x] 5. May Test Connection perform a minimal provider inference that can consume quota or incur cost? — Resolved as LD-018
- [x] 6. What delivery order should govern the implementation tickets after decisions 3–5 are resolved? — Resolved as LD-019
- [x] 7. Which repository and local worktree are canonical? — Resolved as LD-020
- [x] 8. May the legacy migration re-create a profile the user deleted or a credential the user cleared? — Resolved as LD-030
- [x] 9. When the selected provider or model rejects the JSON Schema, does SaySlate fail or retry once without it? — Resolved as LD-031

## Numbered Open Decision Register

Numbers are permanent chat references. Resolved entries remain here for posterity, point to their
locked decision, and are never removed, migrated, reused, reordered, or renumbered. New questions
receive the next integer. Unresolved decisions must not be delegated to a low-reasoning
implementation agent.

### 1. Provider-profile persistence
**State:** Resolved
**Value:** Resolved as LD-014.
**Investigation:** EV-002 found one combined `sayslate-grammar-config` record in `chrome.storage.local`; EV-010 showed that Argus's Electron-only `safeStorage` cannot be reused by a browser extension.
**Why user input is required:** Source inspection established the available mechanisms but could not choose the accepted credential threat model or whether users should re-enter keys after every session; the user has now resolved that choice.
**Recommendation:** Persist versioned, provider-scoped profiles in `chrome.storage.local`, keep the active profile ID separate, and do not introduce a native credential host. This follows SaySlate's existing storage boundary while preventing one provider save from replacing another.
**Evidence:** EV-002, EV-010, and Dustin Thomason's 2026-09-22 clarification.
**Depends on:** —

### 2. Gemini transport
**State:** Resolved
**Value:** Resolved as LD-015.
**Investigation:** EV-003 and EV-005 showed that both SaySlate surfaces already use the working Gemini-native `generateContent` client, while the rough MVP suggested an OpenAI-compatible transport only for the new custom-provider path.
**Why user input is required:** The code proved what works today but could not authorize replacing it merely for transport uniformity; the user has now resolved that choice.
**Recommendation:** Keep the native Gemini client and place new OpenAI-compatible, Anthropic, and custom-provider transports behind a common SaySlate interface. Do not rewrite working Gemini behavior merely to make transports look alike.
**Evidence:** EV-003, EV-005, and Dustin Thomason's 2026-09-22 clarification.
**Depends on:** —

### 3. Long-running inference ownership
**State:** Resolved
**Value:** Resolved as LD-016.
**Investigation:** EV-017 establishes that current Gemini calls are renderer-owned and bounded at 90 seconds; EV-006 shows the service worker owns asynchronous Whisper routing, not AI inference; EV-011 documents the service-worker fetch-response lifetime risk. The current code therefore does not implement a durable AI-request owner.
**Why user input is required:** Source inspection could not decide the expected product behavior when the initiating surface disappears. Requiring survival would create a new durable/offscreen ownership boundary; the user confirmed that preserving today's renderer-owned behavior is appropriate.
**Recommendation:** Preserve renderer-owned inference for this feature: let closing or navigating away cancel the request while retaining the user's source text and last completed result. This supports slow requests while the surface remains open, matches current behavior, and avoids introducing an unproven service-worker/offscreen lifetime mechanism until real usage demonstrates that detached completion is necessary.
**Evidence:** EV-006, EV-011, EV-017, and Dustin Thomason's 2026-09-22 confirmation of decision 3.
**Depends on:** —

### 4. Custom-origin permissions
**State:** Resolved
**Value:** Resolved as LD-017.
**Investigation:** EV-018 establishes that SaySlate currently has a fixed two-origin allow-list, has no optional-host-permission flow, and deliberately fails verification if another origin appears. EV-012 establishes that the new Tailscale/custom-provider request needs a granted host permission.
**Why user input is required:** Both mechanisms can satisfy the network requirement. The user selected preset providers with editable, saved, testable endpoints; least-privilege optional permission for the exact selected origin is the compatible technical consequence.
**Recommendation:** Declare custom HTTPS access as optional and request only the exact configured origin when the user saves or tests that provider. Retain the existing fixed Gemini permission, avoid broad install-time host access, and fail visibly if the user declines the origin grant.
**Evidence:** EV-012, EV-018, and Dustin Thomason's 2026-09-22 provider/endpoint UX direction.
**Depends on:** —

### 5. Connection-test semantics
**State:** Resolved
**Value:** Resolved as LD-018.
**Investigation:** EV-019 found a health endpoint and UI availability projection for Local Whisper, but no AI-provider test path. Argus's production reference validates credentials, endpoint reachability, and selected-model availability through provider-specific listing/health operations, returning precise availability messages without inference (`C:\Argus\runtime\desktop-application.mjs:1709-1737`; `C:\Argus\app.js:781-803`).
**Why user input is required:** The SaySlate code alone could not authorize a potentially billable external call. The user selected Argus as the behavioral reference, resolving the question in favor of non-generative connection testing.
**Recommendation:** Follow Argus's non-generative behavior: validate configuration, reachability, authentication, and exact selected-model availability through provider-specific health or model-list endpoints. Treat structured-output behavior as an inference-path concern, not as part of Test Connection.
**Evidence:** REQ-004, EV-019, the cited Argus production symbols, and Dustin Thomason's 2026-09-22 direction to use Argus as the reference.
**Depends on:** —

### 6. Delivery order
**State:** Resolved
**Value:** Resolved as LD-019.
**Investigation:** The current requirements show provider storage, inference ownership, host permissions, connection testing, and live Tailscale acceptance as separate concerns. Decisions 3–5 can change their ownership and dependency order, and no ticket files or confirmed wave plan exist yet.
**Why user input is required:** The orchestrator could draft a dependency-safe order but required authority to make it the execution contract. The user explicitly delegated that sequencing decision to the orchestrator.
**Recommendation:** Deliver sequentially from shared foundations to live proof: provider-profile schema and migration; permission and adapter boundaries; provider transports; connection testing; provider-selection UI integration; then real Tailscale/LM Studio acceptance. Do not parallelize tickets that share settings, manifest, or request-routing ownership.
**Evidence:** REQ-001–REQ-015, decisions 3–5, and Dustin Thomason's 2026-09-22 delegation of ticket sequencing.
**Depends on:** —

### 7. Canonical repository and local base
**State:** Resolved
**Value:** Resolved as LD-020, superseding the active-source-boundary portion of LD-013.
**Investigation:** EV-008 found that the original OneDrive source had no Git metadata; remote inspection found the existing `SirGunther/SaySlate` repository, and EV-016 verified the audited source import at `C:\SaySlate`.
**Why user input is required:** Multiple possible repository and working-copy locations existed; source inspection could not authorize which one should remain in the active boundary. The user confirmed that the OneDrive copy should now be ignored and may later be deleted.
**Recommendation:** Use `SirGunther/SaySlate` with `C:\SaySlate` as the only active implementation and evidence base. Do not consult the original OneDrive directory during delivery; the user may remove it after validating the canonical repository.
**Evidence:** EV-008, EV-016, and Dustin Thomason's 2026-09-22 clarification.
**Depends on:** —

### 8. Legacy migration after delete or clear
**State:** Resolved
**Value:** Resolved as LD-030.
**Investigation:** LD-023 defines `migrateLegacyConfig` as idempotent, and SAYAI-01 only proves that running it twice creates one profile. EV-002 shows the legacy `sayslate-grammar-config` record still holds `apiKey` and `model`, and nothing records that migration ran. After LD-027's Delete removes the migrated Gemini profile, the next load finds a legacy key and no Gemini profile, so a copy-only migration re-creates it; after Clear Credential, the legacy copy still holds the key.
**Why user input is required:** Not required. LD-027 already fixes that Delete removes a profile and only Clear Credential removes a credential, and REQ-003 forbids one action erasing or restoring another profile's state. The sources settle the outcome; only the mechanism was missing.
**Recommendation:** Move the legacy key and model in the same write that creates the profile, so the absence of those legacy fields is itself the record that migration ran. No new field is added to LD-023's version-1 shape.
**Evidence:** EV-002, LD-023, LD-027, REQ-003.
**Depends on:** —

### 9. Models that cannot enforce the schema
**State:** Resolved
**Value:** Resolved as LD-031.
**Investigation:** The origin asks for "JSON schema whenever possible" ([original ticket](./sayslate-ai-provider-tailscale-original-ticket.md), rough MVP), and LD-003 rejects "Unconditional JSON Schema support". LD-025 fails every request whose provider rejects the schema and rejects "free-form unvalidated fallback", but does not mark LD-003 superseded. Today's Gemini path already accepts the model's free text as the result (EV-003, EV-005), and LD-022 unwraps the structured result to that same plain string.
**Why user input is required:** LD-003 and LD-025 point in different directions, and neither records superseding the other. Which models SaySlate must remain usable with is a product choice the code cannot settle.
**Recommendation:** Keep LD-025's schema request and validation, but when the provider rejects the schema itself, retry once without it and accept only a trimmed, non-empty text reply. This keeps "whenever possible" and LD-003 intact and every model usable. The cost is that the fallback path loses the schema's guard against extra commentary around the text — the same exposure today's Gemini path already has.
**Evidence:** Original ticket, rough MVP; LD-003; LD-022; LD-025; EV-003; EV-005; Dustin Thomason's 2026-09-22 answer choosing the retry.
**Depends on:** —
