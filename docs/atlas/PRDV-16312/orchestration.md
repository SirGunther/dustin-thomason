# Orchestration — atlas/PRDV-16312

Ticket folder: `docs/atlas/PRDV-16312/` (matches the established `docs/atlas/PRDV-XXXXX/` convention used by PRDV-16402 and PRDV-16192, not the skill's illustrative `tickets/<slug>/` path).

Implementation repo: `callisto-back-end` (`C:\Users\dustin.thomason\callisto-back-end`) — recorded, not a docs root.

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | `PRDV-16312-original-ticket.md`, `orchestration.md`, `../PRDV-16312-changelog.md`, `stories/PRDV-16312-job-stories-index.md`, `stories/PRDV-16312-job-story-01-deliverable-reaches-client.md`, `stories/PRDV-16312-job-story-02-new-grouping-is-there-first-time.md` | 2026-08-05 | Ledger reconstructed from disk (pre-skill ticket folder — original ticket was captured before this ledger existed). User confirmed "orchestrate — finish Phase 0". Two stories drafted from the verbatim request (compound-motivation split); 13 open questions carried, none answered by inference. |
| 1 Recon and plan | done | `investigations/PRDV-16312-recon-and-plan.md` (frozen) | 2026-08-05 | Plan approved. Correctly found `collection.created` was removed; reached the **wrong disposition** (story 02 "gated", proposed a projection widening) because the ticket's own cited wiki spec was not read. Corrected at Phase 2 — see the why-log course change. |
| 2 Report | done | `PRDV-16312-investigation.md`, `PRDV-16312-coverage-ledger.md`, `PRDV-16312-diagrams.md`, `PRDV-16312-why-these-changes.md`, `PRDV-16312-future-development-concerns.md`, `testing/PRDV-16312-test-plan.md`, `PRDV-16312-pr-draft.md` (shell), stories 01+02 reconciled + index | 2026-08-05 | Verdict **proceed** — one event, not two. Nine of thirteen story questions closed by evidence. `larry-adams` wiki + design doc are the authority (design Q25: "ClickUp stays wiki-pointer"). |
| 3 Probe & spec | done | `specs/PRDV-16312-locked-decisions.md`, `specs/PRDV-16312-spec.md`, `testing/PRDV-16312-test-plan.md` (refined), stories 01+02 **accepted**, investigation §13 addendum, concern C7 | 2026-08-05 | 15 decisions locked (12 by evidence, 3 grilled). Derrick descoped RabbitMQ → AC6 withdrawn (LD-007). ODP 1.0.7 verified on disk: contract complete, **no ODP change needed** (LD-006). `P3.reconcile` surfaced two gaps the ticket and design both missed → LD-011, LD-012. |
| 4 Prep | done | `PRDV-16312-implementation-plan.md` (staged; written verbatim at Phase 5's first action) | 2026-08-05 | 17 ordered steps, each traced to spec/report/test-plan **and** an acceptance criterion. Includes the new `P5.spec-approved` hard gate at step 2b. |
| 5 Implement | in-progress — **blocked at `P5.gates`** | `PRDV-16312-implementation-plan.md` (frozen), `testing/PRDV-16312-testing-implementation.md`, test plan `executed`; code on `callisto-back-end` branch `PRDV-16312` **uncommitted** | 2026-08-05 | Code, tests and docs done: 74 suites / 367 tests pass, tsc clean, red→green proven, neighbours unmodified. **`npm audit --audit-level=high` exits 1 (pre-existing, 3 high advisories) — a hard stop before commit per `git-commit-workflow`.** So `P5.gates`, `P5.pr` and `P5.advance` are not done. `P5.spec-approved` was satisfied by **waiver**, not approval. |
| 6 Manual review | pending | | | |

Resume: Phase 5 — Working mode, **gated on Larry's response to `larry-adams` PR #34**

## Ledger notes

- **`P5.spec-approved` — WAIVER, not approval (2026-08-05).** The gate's `done` condition permits proceeding on a recorded waiver naming the authoriser and the risk. Both are recorded here.
  - **Authorised by:** Dustin Thomason, explicitly — *"we're gonna actually go ahead and work ahead fairly certain given that we already have a spec and what needs to be done."*
  - **What was waived:** Larry Adams' response to `larry-adams` PR #34. The PR remains open and unreviewed.
  - **Basis for it being reasonable:** the authoritative spec already exists and was authored by the reviewer; the investigation requested **no changes** to it; and the ODP contract was verified complete on disk. The three addendum items extend the spec rather than contradict it.
  - **Residual risk, itemised — this is what the waiver actually costs:**
    - **LD-012 / concern C7 (the material one).** Callisto will send `deliverableCollectionValue` for **static** collections whose rows Dione's own migrations seed. Inert if Dione's upsert is id-keyed and writes the name on insert only; if it overwrites, a routine upload silently renames a seeded collection for every client. Unverifiable from this repo, and the RabbitMQ descope (LD-007) removed the observation step that would have caught it. **Mitigation if it lands wrong:** gate on `collection_kind = 'dynamic'` — a one-line change. Agent recommended against waiving this specific item; the user reaffirmed.
    - **LD-013.** Typing the converter against ODP's exported type deviates from every existing producer. Low risk — strictly stricter than precedent — but if Larry prefers repo consistency, the converter's return type changes.
    - **LD-011.** Adds a repository read on the by-id branch. Low risk; satisfies an AC that was otherwise unsatisfiable.
  - **If Larry's response contradicts any of the three,** the code changes and the corresponding `LD-###` row is superseded on the record with a why-log course change — not edited silently.

- **Skill gap found and fixed mid-run (2026-08-05), at the user's instruction: the lifecycle had NO spec-approval gate.** `steps.csv` contained zero human approval/review steps — the only approvals in all 49 steps were the two plan-mode approvals (Phases 1 and 4), which approve *plans*, not the spec. Nothing between `P3.spec` and `P5.code` required anyone to approve the design. That contradicted the documented PRDV process (spec → review by the principal dev → implement only after approval).
  - **Fix:** added participant `REV` (spec reviewer) plus two steps — `P3.spec-submit` (seq 65, `notify AG→REV`) and `P5.spec-approved` (seq 20, `read AG→REV`, the hard gate immediately before `P5.code`). Both `check_by: human`. Wired into `SKILL.md`'s phase map, Phase 3 and Phase 5 Do lists, and the Do-not list. `verb` is a validated closed set, so existing verbs were used rather than inventing `submit`/`confirm`.
  - **Verified:** `render-sequence.ps1 -Check` → *validation OK, 51 steps, 22 participants*; `validate-workflows.ps1` → 0 errors; `sync-rules.ps1 -Check` clean under 5.1 after regeneration.
  - **Retroactive effect on this ticket:** `P3.spec-submit` did not exist when Phase 3 was marked `done`, so it was reopened and satisfied — see below. Phase 3 stays `done`.
- **`P3.spec-submit` satisfied 2026-08-05 — `larry-adams` PR #34.** Larry already authored the authoritative spec for this ticket, so the submission is an **addendum** rather than a competing spec: `PRDV-16312-implementation-decisions-addendum.md`, carrying only what the investigation added (LD-011, LD-012 + risk C7, LD-013's precedent deviation), two payload hazards, and four documentation defects. Wired into the vault per `AGENT.md` — index entry in `systems/README.md`, reciprocal wiki-link and frontmatter bump on Larry's spec, no content of his changed. **No reviewer requested** on the PR, per `git-commit-workflow`.
- **Correction to an earlier claim in this run's artifacts:** the coverage ledger and concern C2 described `larry-adams` as "read-only" / "never a push target". That is **too broad and wrong**. Verified in its history: squash-merged spec PRs (`PRDV-15591 … (#7)`, `PRDV-15619 … (#8)`), a real review cycle (`af5680c … Remove polling spec per review`), and 12 prior commits by the user. The narrow restriction is changelog/workflow bookkeeping files, not specs.
- **Rebase hazard caught before committing:** `git pull --ff-only` printed `Updating 559c481..318bd0a` then **`Aborting`** (uncommitted local edits), so the branch was initially cut from stale `main`. Upstream `318bd0a` — "PRDV-15736: remove rabbitMQ work from scope of epic" — had **already applied Derrick's descope** to nine specs including this one, removing the dev-queue AC and replacing the manual step with *"confirm an outbox row is written with the expected routekey + payload"*. Independent confirmation of LD-007 and of test-plan M2 as the terminal verification. Branch was rebased onto `318bd0a` and the addendum's §6 rewritten to reference the applied change rather than announce it.
- **Vault defect found, deliberately not fixed:** `systems/README.md` links all 10 epic specs under `emit-grant-events/`, but that folder was renamed to `epic-PRDV-15736-make-atlas-metadata-available-to-planet-portal/` (`R100` renames). 10 broken wiki-links, and the source of the ClickUp ticket's dead `## Wiki` path. Flagged in the PR body and addendum §7d rather than bundled into a spec-review diff.

- **Open skill assumption RESOLVED (2026-08-05): scripts CAN run while genuinely in Plan mode.** `SKILL.md`'s Mode handling section records this as untested and asks for the observed result. Observed in Claude Code: `Bash` calls (git, grep, find, `render-sequence.ps1`) all executed normally during Phase 1 while Plan mode was active; only **writes** were blocked, and the plan file was the sole permitted edit. So the deferred-notification pattern is not strictly required in this harness — though it remains correct as a portable default for Cursor/Codex, which were not tested.
- **`larry-adams` must be a Phase 1 input when the ticket names a wiki path.** This run read it only at Phase 2, after the user asked. The path was captured verbatim in `PRDV-16312-original-ticket.md` under `## Wiki` and Phase 1 did not follow it, which produced a wrong disposition on half the ticket. The `spec-writing` and `personal-methodology` rules already treat `larry-adams` as read-only spec input; the gap was procedural, not a missing rule. Candidate improvement to `steps.csv` `P1.consult`: make "read the ticket's cited wiki spec" an explicit part of the step's `done` condition.
- **`callisto-back-end/node_modules` was emptied by this run and is not restored.** `npm ci` cleared it then failed E401. Blocks all Phase 5 gates. See concern C6 / report OV-1.

- **2026-08-05 — reconstruction.** Invoked against an existing folder holding only `PRDV-16312-original-ticket.md` and `RABBITMQ_CONFIG_REQUEST_TEMPLATE.md`. Per the resume protocol (case 3, ledger missing but artifacts exist), state was reconstructed from disk and confirmed with the user before proceeding. `P0.ticket` was already satisfied; `P0.ledger`, `P0.changelog`, `P0.stories`, `P0.advance` were open.
- **`RABBITMQ_CONFIG_REQUEST_TEMPLATE.md`** sits in this folder unfilled. The ticket's Wiki line names "Dev Note: RabbitMQ Config", so the filled request is expected output of a later phase (topology for the two new routekeys and Dione's consumption of them). It is a reference template today, not a produced artifact.
- **No `docs/callisto/` exists** in this repo; all PlanetDepos ticket memory lives under `docs/atlas/`, including backend-only tickets (see PRDV-16216). Changelog therefore goes to `docs/atlas/PRDV-16312-changelog.md`.
- **Tooling defect — `check-steps.ps1` could not see a PRDV-prefixed original ticket. FIXED 2026-08-05 at the user's instruction, before Phase 1.** `steps.csv`'s `TICKET` participant carried the literal path `original-ticket.md` with no `<...>` placeholder, and `check-steps.ps1` only wildcards placeholders (`[regex]::Replace($p.path, '<[^>]+>', '*')`). The skill's own layout section permits the prefix ("PRDV tickets may prefix artifact filenames with `PRDV-XXXXX-`"), so `P0.ticket` reported `MISSING` on every PRDV ticket even when the artifact was present and correct.
  - **Fix:** `agents/skills/orchestrate/steps.csv` — `TICKET` path is now `<prefix>original-ticket.md`, using the placeholder mechanism already in the script rather than adding new script logic. `agents/scripts/sync-rules.ps1` regenerated the `.cursor/` and `.claude/` mirrors.
  - **Scope check:** `TICKET` was the only defective row. Every other `ticket-artifact` participant either carries `<slug>` (globs correctly) or has a genuinely fixed name (`orchestration.md`).
  - **Verified:** `P0.ticket` now reports `REVIEW` (found, content needs human judgment) instead of `MISSING` on both PRDV-16312 and PRDV-16402; a synthetic unprefixed `original-ticket.md` still matches, since `*` matches zero characters, so personal-project naming is unaffected. `validate-workflows.ps1` exits 0.
- **Separate pre-existing bug, NOT fixed, NOT introduced by this run — `sync-rules.ps1` staleness check is interpreter-dependent.** `agents-skills.md` is reported `out of date` by whichever of Windows PowerShell 5.1 / pwsh 7 did not generate it; each rejects the other's output. Proven pre-existing by running `-Check` under both against clean `HEAD` (5.1: up to date; pwsh 7: stale). All `agents/skills/**` sources are valid UTF-8, so the input is not corrupt; the suspect is `Read-Utf8`'s `[System.IO.File]::ReadAllText($Path)` called with no explicit encoding, inheriting interpreter-default decoding. The repo's canonical generator is **5.1** — both the git hook and `validate-workflows.ps1` invoke it via `powershell.exe` — so this run regenerated with 5.1 to match the committed state. Fixing it means touching the generator that produces every rule, skill, and doc output, which is its own change; carried to the Phase 6 cruft check.
- **Phase 0 gate evidence.** `check-steps.ps1 -ThroughPhase 0`: `P0.ledger` VERIFIED; `P0.stories` REVIEW (authored — reviewed against its `done` condition: both stories are `draft`, both have index rows, and no criterion names a design element); `P0.changelog` and `P0.advance` are `n/a` to the script by design (changelog lives outside the ticket folder; advance is an actor); `P0.ticket` MISSING is the tooling defect above, not a gap. No `callisto-back-end` file created, edited, or run.
