# Orchestration — atlas/PRDV-16461

**Non-canonical location note:** this ticket's artifacts live at `docs/atlas/PRDV-16461/` rather than the skill's default `docs/atlas/tickets/<slug>/` layout. Original-ticket.md and the decision-support prototype already existed here before orchestration was invoked; the user explicitly chose (2026-09-03) to keep this location rather than migrate. All subsequent phase artifacts follow the same subfolder shape (`stories/`, `investigations/`, `specs/`, `testing/`, `dnu/`) under this root instead of under `tickets/<slug>/`.

| WorkLists card | todo-1788443447379-798e4585 |

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | `PRDV-16461-original-ticket.md` (pre-existing), `stories/PRDV-16461-job-story-01-default-collection.md`, `stories/PRDV-16461-job-story-02-default-indicator.md`, `stories/PRDV-16461-job-stories-index.md`, this ledger | 2026-09-03 | original-ticket.md and the decision-support prototype (`PRDV-16461-default-collection-prototype.html`) were captured in a prior session (2026-09-02, pre-orchestration) and reused as-is per the artifact-already-exists edge case. Two job stories drafted this session: 01 (default collection pre-selection, complete AC, 2 code-discoverable open questions) and 02 (visual indicator, still-open product decision, explicitly deferred by user to pre-implementation). Changelog (`docs/atlas/PRDV-16461-changelog.md`) read and aligned — Current state, Plans (A/B/prototype), and prior session log carried forward. |
| 1 Recon and plan | done | plan staged (saved verbatim to `investigations/PRDV-16461-recon-and-plan.md` at Phase 2's first action) | 2026-09-03 | Recon traced `atlas-front-end` upload-form composable + `callisto-back-end` seed migrations. Resolved both ticket-flagged unknowns by evidence (F1: `Full Transcript`/`MP4 Video` are seeded production statics with eligible types configured). Key finding F3: a partial default already exists at `useDeliverableFileUploadForm.ts:407-414` and only fires when a track has exactly one static collection — the work widens an existing rule. **Team review caught a material error:** my F6a claimed generic drag-and-drop needed no work; the code refutes it (form opens with no track, group headers are `disable: true`), so the reactive-default AC was unmet. User direction: implement the AC rather than revise it — spec a minimal explicit track-selection state for generic DnD, with a bounded out-of-scope list. Two further claims retracted after review (F8a: catalog is not newly narrowed; A3 downgraded from a spec gate). A8 reopened as a consequence of the DnD change. Artifact scope: user chose the full mandated package. Estimate risk flagged — change now exceeds a pure mapping, revisit 3-point sizing at Phase 4 |
| 2 Report | done | `investigations/` — `PRDV-16461-recon-and-plan.md` (frozen), `-investigation.md`, `-coverage-ledger.md`, `-diagrams.md`; `testing/PRDV-16461-test-plan.md`; `PRDV-16461-why-these-changes.md`, `-future-development-concerns.md`, `-pr-draft.md` (shell); `stories/` reconciled; changelog session log | 2026-09-03 | Plan approved after three review passes. Review 1 caught the unmet generic-DnD AC (F6a retracted); review 2 caught under-specified reactive wiring (F6b), a test plan contradicting its own workflow (`spec:445`), and the recategorize singleton hole (F4a); review 3 added the interaction contract (F6c), narrowed the transition to mutable DnD track changes (F6b.1), and fixed resolver precedence (F6d) — the last closed a real bug where a mapped-but-missing collection would have fallen through to the singleton fallback. A8 and F5a both closed. Carry-forward rule added: the spec states resolved positions only, no retraction narrative |
| 3 Probe & spec | done | `specs/PRDV-16461-locked-decisions.md`, `specs/PRDV-16461-spec.md`, `stories/` (01 **accepted**, 02 held `draft`), `PRDV-16461-future-development-concerns.md` (+C4b) | 2026-09-03 | Reconcile resolved the DnD track-control shape from precedent (`useTrackSelectorForm.ts` / `TrackSelectorForm.vue`) and **reversed a Phase 1 decision**: unpermitted tracks are shown disabled with the existing `noTrackPermission` tooltip, not hidden — both sibling controls already share that i18n key (LD-004). Grill-me: only two questions passed the gate; one of those the user correctly identified as already settled by the AC (LD-005, no acknowledgement state). 15 locked decisions recorded with 7 question-gates documented as resolved-without-asking. Story 01 accepted at 14 criteria, 0 open questions; story 02 deliberately held at `draft` (mechanism is an unresolved Product decision, owner named). Spec written to the carry-forward rule — verified free of retraction narrative. **`P3.spec-submit` done** — spec submitted as [atlas-front-end #565](https://github.com/planetdepos/atlas-front-end/pull/565), SHA `68506740701d4b31493cf9285fb503fe2fa8ec66`. Placement corrected twice before landing (wiki → callisto → atlas); `atlas-front-end/docs/specs/README.md` settles it: entirely-UI tickets live in Atlas. PR body trimmed to description-only at user direction — a spec PR carries no test evidence because it contains nothing executable. Test plan refined and frozen; reviewer caught NP-7 contradicting LD-004, and the sweep found two further defects (LD-005 unasserted, eight drifted criterion citations) |
| 4 Prep | done | `PRDV-16461-implementation-plan.md` (saved verbatim at Phase 5's first action, frozen) | 2026-09-03 | Four ordered steps, each traced to a spec section, a locked decision, and a test-plan scenario: (1) the mapping module, (2) resolver precedence + recategorize guard, (3) DnD track state + options scoping, (4) the track control component. Riskiest work last, against a proven base. No re-investigation — planned from the artifacts. Reuse identified rather than rebuilt: existing dynamic-state clearing watcher (`:571-583`), per-file type refresh (`:735-746`), all four i18n keys, and the existing permission call. Estimate flagged: steps 1–2 fit 3 points, steps 3–4 do not |
| 5 Implement | done | `PRDV-16461-implementation-plan.md` (frozen); `testing/PRDV-16461-test-plan.md` (executed); `testing/PRDV-16461-testing-implementation.md`; atlas-front-end commits `b8841a70` (spec correction), `8a1a03ff` (implementation); [PR #565](https://github.com/planetdepos/atlas-front-end/pull/565) | 2026-09-03 | Spec-review gate satisfied by **user waiver, not approval** — see the gate table below. **The spec was found wrong during implementation:** LD-003's two-part generic-DnD discriminator misclassified direct upload and failed 12 existing tests; corrected to three parts, amended on the record, pushed to #565 before continuing. A second plan decision was reversed by precedent (LD-004, unpermitted tracks shown disabled rather than hidden). Pre-existing recategorize defect confirmed **red-before-green** against unmodified source. Gates: audit fail (pre-existing, waived), lint pass, types pass, tests pass 1261/1265 across 141 files; touched area 99 passing across 3 files including the form's first component spec. **Manual verification NOT performed** — recorded as outstanding, not implied complete |
| 6 Manual review | pending | | | |

Resume: Phase 6 — Working mode

## Spec-review gate (Phase 5 prerequisite)

| Field | Value |
| --- | --- |
| Spec PR | [atlas-front-end #565](https://github.com/planetdepos/atlas-front-end/pull/565) |
| Submitted | 2026-09-03, SHA `68506740701d4b31493cf9285fb503fe2fa8ec66` |
| Reviewer response | **None received.** Re-checked at Phase 5 start (`gh pr view 565`): state `OPEN`, `reviews: []`, `mergedAt: null`. The single comment is an automated ClickUp task-link post by a bot — **not** a human review response, and must not be read as one |
| How the gate was satisfied | **User waiver, 2026-09-03** — "let's assume this PR is accepted, we can move forward with the tooling at this time and adjust if needed" |
| Authorised by | Dustin Thomason (ticket assignee) |
| Risk carried | Implementation proceeds against an unreviewed design. If the reviewer later disputes the resolver precedence (§4.3), the recategorize guard scope (§4.4), or the new drag-and-drop track control (§4.5), rework lands on code already written rather than on a document. The track control is the largest exposure — it is new UI, not a widened rule. |
| Revisit | Any substantive review comment on #565 reopens the affected locked decision and this waiver |

**Phase 2 verification:** `check-steps.ps1 -ThroughPhase 2` → 10 VERIFIED, 8 n/a (outside the ticket folder or not files), 7 REVIEW (Phase 1 judgement items, all satisfied in the approved plan). Report verdict: **proceed**, no open variables. Board: three Investigation rows marked with evidence; "Contact relevant parties" and "Loop in Pair Programmer" left **unmarked** — neither happened, and both ticket unknowns resolved from code instead.

**Carried into Phase 3:** (a) the spec states resolved positions only — no retraction narrative, per the recon's carry-forward rule; (b) read `TrackSelectorForm.vue` before specifying the new DnD track control, to reuse the existing shape rather than invent one (coverage-ledger frontier); (c) the 3-point estimate is revisited at Phase 4.
