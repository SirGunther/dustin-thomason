# Orchestration — atlas/PRDV-16403

| WorkLists card | none — not supplied at invocation (see Phase 0 notes) |

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | `docs/atlas/PRDV-16403/PRDV-16403-original-ticket.md`, `docs/atlas/PRDV-16403/orchestration.md`, `docs/atlas/PRDV-16403-changelog.md`, `docs/atlas/PRDV-16403/stories/` (3 stories + index) | 2026-08-18T19:54:06Z | Ticket was captured from the active ClickUp page in a prior session; this session completed the rest of Phase 0. **Folder convention** follows the atlas precedent `docs/atlas/PRDV-XXXXX/` (PRDV-16402, PRDV-16192, PRDV-14055), **not** the skill's nominal `docs/atlas/tickets/<slug>/` — disk facts win; divergence already logged as a cruft-check candidate on PRDV-16402 and re-observed here. **Capture gap recorded:** the artifact's *Explicit Constraints In Original Request* and *Context Paths In Original Request* sections carry placeholder text only; the substantive links (ClickUp `https://app.clickup.com/t/43227262/PRDV-16403`, Figma `node-id=10285-154183`) are preserved inside Original Request and reproduced in the changelog Context. Original Request **not** rewritten. **Related-spec search completed (user-requested):** `larry-adams` no longer holds the GCA specs — `systems/neptune/callisto/README.md` records them as moved into the implementing repo. The authoritative parent-epic spec is `callisto-back-end/docs/specs/atlas-client-access/contacts/5-story-PRDV-14828-view-warnings-in-access-manager.md` (author **Derrick Dieso**, created 2026-07-27), present on `origin/main` (`631ed42e`) but **absent from the local callisto tree**, which sits on branch `PRDV-16313` (`c43be32c`). Sibling `6-story-PRDV-16391-replicate-case-warning-and-remarks-to-callisto.md` is the blocking data story. **Read-only touch-point verification ran this session** (no file written in either implementation repo; atlas-front-end on `main` @ `02c98e1e`) — results and the five deltas it surfaced are recorded in the changelog Context and carry into Phase 1 as recon input, **not** as an approved plan. No implementation-repo file touched; no branch created. **CORRECTION 2026-08-18T21:10:00Z (Phase 1):** this row's blocking-dependency claim was **wrong**. It read the local callisto tree, which sits on branch `PRDV-16313` (`c43be32c`), branched *before* the merge. **PRDV-16391 has already landed on `origin/main`** — commit `53d961ed` is an ancestor, migration `1786036989067-alter__add_warning_remarks__cases_table.ts` adds `cases.warning` (varchar NULL), `cases.remarks` (text NULL) and `cases.remarks_html` (text NULL), and `Case` carries all three (L37-42). The ticket's whole *Sequencing note* is therefore moot; all four fields build in one pass. What remains open is **PRDV-16392** (DMS CDC column mapping), which governs whether real case data *arrives* — code unblocked, data not guaranteed. Original text left standing above per the no-silent-rewrite rule. |
| 1 Recon and plan | done | approved recon-and-plan (`docs/atlas/PRDV-16403/investigations/PRDV-16403-recon-and-plan.md`, saved verbatim + frozen at Phase 2's first action; byte-identical to the approved plan) | 2026-08-18T21:10:00Z | Ledger row, why doc and story reconcile were `deferred (plan mode)`, materialized as Phase 2's first action; the deferred Phase 1 notification fired batched with Phase 2's start. **Consult ran first** across all five ledgers plus — newly — `docs/atlas/reviews/`, which the ledger glob misses because it is not ledger-formatted and which turns out to be the **only** prior coverage of `granting-client-access/contacts/` anywhere. Two prior claims **reopened and corrected**: 16313's *'no feature flags in granting-client-access'* (superseded by `IS_CLIENT_ACCESS_OUTBOX_ENABLED`, PRDV-16310) and 16312's GCA-flag manual-test blocker (became **F9**). **Class reframed** from *new capability* to **completion of a deliberately deferred read path** — the plumbing is a mirror exercise against a known-good template under machine-enforced fitness functions, so risk sits elsewhere. **Wedge revised on the record** from *ship Contact+Firm first* (withdrawn once the blocker proved false) to *the sanitised-render + empty/error-state panel*, the only part with no in-repo precedent and no prior investigation. **Nine findings**, four of them corrections to the ticket's own prescriptions: **F9** manual verification is blocked and the workaround PRDV-15776 documents does not exist (`IsFeatureAllowedTS` is Cognito-claims-only by explicit JSDoc; no override in either repo); **F1** `null` means both *RB holds nothing* and *DMS never mapped this* so the *No warning info* copy would state something false; **F7** the ticket names the wrong sibling to mirror (needs `FetchClientAccessListAction` — mapper, guard, strict pipe — not the grants action which has none of the three); **F8** its *reuse ValidateContactExists for 404* is unreachable, that validator throws `BadRequestException`; **F6** `.rightColumn` is `overflow: hidden` so the internal-scroll criterion needs a new inner element; **F2** the overlay unmounts on close so only `gcTime: 0` of the three proposed cache options does any work. Seven questions closed by evidence; **seven genuine decisions (D1-D7)** carried with owners. Three read-only Explore agents; **no implementation-repo file touched** — `atlas-front-end`, `callisto-back-end` and `larry-adams` all verified clean after the pass. |
| 2 Report | done | `investigations/PRDV-16403-investigation.md`, `investigations/PRDV-16403-coverage-ledger.md`, `investigations/PRDV-16403-diagrams.md`, `testing/PRDV-16403-test-plan.md` (`seeded`), `PRDV-16403-why-these-changes.md`, `PRDV-16403-future-development-concerns.md`, `PRDV-16403-pr-draft.md` (shell) | 2026-08-18T21:25:00Z | Deferred Phase 1 writes all materialized here: plan saved verbatim (byte-identical, `diff -q` clean), why doc created, story reconcile applied with a Phase 1 Story log entry on all three. **The §0 corrections were applied to this ledger and the changelog as dated corrections with the original text left standing** — no silent rewrite. Report disposition **proceed with conditions**; §5 **links** the diagrams artifact and embeds none. Coverage ledger carries the Consulted table first (six sources, two claims reopened-and-corrected under conditions 1 and 2), eight areas, seven frontier items; area 8 is honestly recorded as **read, not executed** — no gate command was run this phase and none is claimed. Diagrams: current-vs-target, the null-provenance flow (F1), and the unmount/remount sequence (F2), with state and ER diagrams carrying explicit N/A lines. Test plan `seeded` with 25 scenarios, each naming its criterion or flagged `[NO-CRITERION]`; NP-3 and manual M-5 are marked load-bearing. Four concerns opened — C1 firm-identity mismatch (the plan's named candidate), C2 unguarded sibling read action, C3 inherited guard smell, C4 manual verification blocked. PR draft is an **unfilled shell**, headings only, with a note saying so. No implementation-repo file touched. |
| 3 Probe & spec | done | `specs/PRDV-16403-locked-decisions.md`, `specs/PRDV-16403-spec-addendum.md` | 2026-08-18T22:05:00Z | Auto-advanced from Phase 2. `P3.reconcile` closed the last code-discoverable question — both `ContactsRepository` and `ClientAccessListRepository` use the same `firm.id = contact.account_id` join, so the firm-identity mismatch (F3) is a narrow race, not a live defect; concern **C1 downgraded** and the frontier item closed. **grill-me halted after one question:** Dustin deferred LD-001 (empty-vs-unavailable copy) to research the PRDV-16392 dependency, then **accepted Derrick Dieso as the design authority**. That ruling locked LD-004 (non-blocking) and LD-006 (bare sanitiser — the ClickUp allowlist is *not* Derrick's design) outright, and LD-008 (addendum, not a new spec). LD-002/003/005 are **provisional-and-flagged**: his spec is silent or self-contradictory there, so authority cannot settle them. **Spec output is an addendum**, not a spec — his `5-story-PRDV-14828` is authoritative and `docs/specs/README.md` forbids duplication. **Verification verdict recorded plainly: his spec is valid with four named exceptions**, two of which change observable behaviour. **Spec provenance stated in the changelog** after Dustin flagged it had never been confirmed — no spec for this ticket was authored by the agent. `P3.spec-submit` **not done** — Dustin owns delivering the addendum to Derrick. |
| 4 Prep | skipped (Dustin: move straight to implementation, Derrick's spec accepted as authority) | | 2026-08-18T22:05:00Z | **Downstream inputs now missing, named per the skill:** no approved implementation plan exists, so there is no frozen record of approved sequencing and no plan-step-to-test-plan trace; Phase 5 proceeds against the addendum and the seeded test plan instead. The test plan was **not** refined at Phase 3 either, so it remains `seeded` — its scenarios were written before the locked decisions and NP-5's expected status code is still a blank pending LD-002. |
| 5 Implement | in-progress | branch `PRDV-16403` in both repos; 8 new + 6 modified files in `callisto-back-end`; 4 new + 2 modified in `atlas-front-end` | 2026-08-18T22:05:00Z | **Backend slice complete and green.** Both repos branched off `main` (callisto `631ed42e`, atlas `02c98e1e`) — the merge was mandatory, `Case.remarksHtml` does not exist on the prior branch. Built on the **`FetchClientAccessListAction`** template per addendum deviation 3, not the grants action the ticket names. Guard added (deviation 2), 400 accepted over 404 (deviation 1). Mapper sits in the **application** layer with the action mapping, as the fitness functions require. **Frontend data layer complete**; the **panel component is deliberately not built** — it is where LD-001, LD-005 and LD-006 concentrate, and LD-001 is still deferred. **One neighbour broken and fixed:** adding a constructor dependency to `ContactsService` broke `contacts.service.spec.ts`; provider added, suite green. **`npx tsc --noEmit` does not pass on `main`** — 11 files fail on stale `@planetdepos/orbital-docking-protocol` exports; proven pre-existing by intersecting the error-file set with my changed-file set (empty). Two GCA suites fail for the same reason, in files never touched. **Not done:** repository integration spec + the four seeder extensions it needs, and the panel. Nothing committed. |
| 6 Manual review | pending | | | |

Resume: Phase 5 — Implement (Working mode) — backend done, panel blocked on LD-001

## Ledger notes

- **WorkLists card — path taken:** neither path was available. The user supplied no card id at invocation, and no WorkLists card-creation tooling is reachable in this session (no WorkLists MCP tool is present; the `claude.ai` and `Microsoft 365` MCP servers are unauthenticated and cannot be authorized from a non-interactive session). Per `worklists-card-sync`, the card id is **never searched for** — so this is recorded as `none` rather than guessed. Board sync is therefore `skipped (no WorkLists card)` for every phase until an id is supplied; supply one at any phase and sync resumes from there.
- **Two implementation repos.** This story spans `callisto-back-end` (new read endpoint) and `atlas-front-end` (panel + composable + i18n). The changelog lives under `docs/atlas/` per the ClickUp system, and both repos are named in its Ticket metadata.
- **Blocking dependency is real, verified in code.** `callisto-back-end/src/cases/domain/entities/case.entity.ts` declares `@Entity('cases')` with **no** `warning` and **no** `remarks_html` column. Case Warning + Case Remarks cannot return data until PRDV-16391 lands; Contact + Firm can ship immediately (`contact.entity.ts` L88 `warning`, `firm.entity.ts` L51 `warning`).
- **Open Plan-mode question (per the skill):** whether scripts can run while genuinely in Plan mode remains untested in this harness. This run keeps the deferred-notification pattern rather than testing it; if a Plan-phase script call is attempted, record the observed result here.
