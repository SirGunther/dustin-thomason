# PR draft — PRDV-14184

> **UNFILLED SHELL.** Staged at Phase 2 so the structure exists; the body is written at **Phase 5**, after the change is implemented and verified. Scope can still move at Phases 3–4, so do not fill this early. Content comes from [the testing-implementation doc](./testing/PRDV-14184-testing-implementation.md) (test evidence) and [the why doc](./PRDV-14184-why-these-changes.md) (description), per `agents/docs/pull-request-workflow.md`.

## Title

`PRDV-14184: <short imperative description>`

## Clickup

https://app.clickup.com/t/43227262/PRDV-14184

## Description

_<what changed and why — from the why doc's categorized change breakdown; bullets welcome>_

```
<40-character tip commit hash — git rev-parse HEAD after push>
```

## Test Evidence

_<from the testing-implementation doc: the scenarios stress-tested and what proved them. Screen recording preferred over a still — a text cursor may not render in a screenshot. Include the serial gate results: audit → lint → tests, with exact command + scope + result.>_

## Checklist

- [ ] _<repo template boxes — fill from `.github/pull_request_template.md` in atlas-front-end at PR time>_

---

**Reminder at fill time:** no reviewer is requested through GitHub's reviewer mechanism unless the user explicitly asks in that moment (`git-commit-workflow`).
