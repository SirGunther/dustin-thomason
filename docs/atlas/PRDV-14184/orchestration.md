# Orchestration — atlas/PRDV-14184

> **Location corrected 2026-09-14.** These artifacts were initially written to `docs/atlas/tickets/focus-proceeding-name-on-create/`, following the orchestrate skill’s generic `tickets/<slug>/` layout. That is **not** this repo’s convention: every sibling ticket (PRDV-14055, 16192, 16312, 16313, 16402, 16403, 16461) lives at `docs/atlas/PRDV-XXXXX/`. Worse, `PRDV-14184-original-ticket.md` was **already** in the right place and was moved out of it at Phase 0 and labelled “non-canonical” — exactly backwards. Everything is now back under `docs/atlas/PRDV-14184/` and the `tickets/` folder is removed.

| WorkLists card | todo-1789145480003-4fc6a07d |

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | original-ticket.md (relocated from non-canonical `docs/atlas/PRDV-14184/`); stories/PRDV-14184-job-story-01-focus-name-field.md; stories/PRDV-14184-job-stories-index.md; docs/atlas/PRDV-14184-changelog.md | 2026-09-11 | original-ticket.md pre-existed at a non-canonical path from a prior session (ClickUp capture already complete, verbatim request + AC present); moved to canonical `tickets/<slug>/` layout, no content changed. Repos aligned: atlas-front-end switched from PRDV-16461-implementation to main (fast-forwarded 28 commits); callisto-back-end already on main (has pre-existing unrelated local mods to .swcrc/notification-template-preview.html/package-lock.json — left untouched, not part of this ticket). **WorkLists board:** card `todo-1789145480003-4fc6a07d` confirmed via `GET /todos/{id}` — path A (user-supplied id). Wrote nothing this phase. ~~Recorded here as a "no checklist structure" guard stop because the card *body* is the raw `"# Ticket Template"`.~~ **That was wrong and is corrected at Phase 2:** the checklist lives in an attached **note**, not the card body, and `GET /api/notes` shows two notes on this card (a full phase checklist and a Current Step / Waiting On / Next Up note). The Phase 0 check inspected only the card body and should have inspected its notes. No guard ever actually fired. |
| 1 Recon and plan | done | investigations/PRDV-14184-recon-and-plan.md (approved plan, saved verbatim, frozen) | 2026-09-11 | Recon executed via two read-only Explore agents (surface enumeration; focus-idiom/API/test-convention sweep) plus direct reads of both target components, `Overlay.vue`, `AddNewProceeding.vue`, the existing overlay spec, Quasar `QInput` types, and the callisto create-proceeding path. Load-bearing finding: `Overlay.vue` hides with `v-show` and `NewProceedingsOverlay` is mounted with no `v-if`, so a plain `autofocus` fires once at Job Detail page load into a hidden field and never on overlay open — the naive one-line fix half-lands. Recon conflict between the two agents over whether `autofocus` re-fires for index-keyed appended rows was resolved in the plan (it does fire on append; the disqualifier for S1 is the `v-show` container) and carried as an assumption for the red→green test. Callisto ruled out: proceeding `value` is client-supplied on both create DTOs with no server-side default. |
| 2 Report | done | investigations/PRDV-14184-investigation.md; investigations/PRDV-14184-coverage-ledger.md; investigations/PRDV-14184-diagrams.md; PRDV-14184-why-these-changes.md; PRDV-14184-future-development-concerns.md; testing/PRDV-14184-test-plan.md; PRDV-14184-pr-draft.md (shell); stories/ reconciled; docs/atlas/PRDV-14184-changelog.md (session log) | 2026-09-11 | Verdict: **proceed with conditions**; class held (capability gap, no reframe). **Self-correction logged:** mid-phase I claimed the proceedings uniqueness constraint was case-insensitive on the strength of its name; reading migration `1766801287907` refuted it (plain case-sensitive `UNIQUE`, while sibling `records` implements `LOWER("value")` correctly). Corrected in report §7 + coverage area 9, raised in the concerns doc, logged as a Phase 2 course change in the why doc. **Board:** checklist note patched with 6 rows completed by Phases 0–2 (batched, `lastModified` precondition honored); Current Step / Waiting On / Next Up written; **Work Ahead left untouched** (user's field). Unmarked rows reported in chat. **Status transition skipped (not a stop):** `PATCH /todos/{id}/status` → 400 "Task status is not available for this card's color tags" — the card has `tag: null` and the server gates status on color tags; assigning a tag is not an agent decision, so `Unrefined` stands. |
| 3 Probe & spec | done (4 review rounds; scope reopened) | specs/PRDV-14184-locked-decisions.md (LD-001-008 + question gates); specs/PRDV-14184-spec.md (pointer); atlas-front-end/docs/specs/atlas-maintenance/proceedings/PRDV-14184-focus-proceeding-name-on-create.md (the spec) + docs/specs/README.md index entry; stories/ accepted; testing/PRDV-14184-test-plan.md (refined); why-doc Phase 3 entry; concerns Concern 3 addendum | 2026-09-11 | **All four grill-me questions closed on evidence; none escalated.** LD-001 scope = both surfaces, derived from the AC being universally quantified over the user action (narrowing would require rewriting the criteria, not merely doing less work). LD-006 closed investigation assumption A5 (nextTick vs setTimeout) on Vue/Quasar source evidence, keeping a tuned delay out of the diff entirely. LD-005 drops the q-input stub after finding it has no focus() method - which would have made every focus test pass vacuously; the same read found an existing test passing for the wrong reason. **Spec placement:** filed in atlas-front-end per that repo README (entirely-UI ticket routes there); pointer kept at specs/PRDV-14184-spec.md. **Board: skipped (WorkLists server unreachable, curl exit 7)** - a skip, not a stop, per worklists-card-sync; no notification sent. **P3.spec-submit done:** committed `e9844c5d` on `PRDV-14184-spec`, pushed, spec PR [#572](https://github.com/planetdepos/atlas-front-end/pull/572) opened with no reviewer requested. **Audit gate FAILED (11 high, pre-existing) and was WAIVED by the user** for this docs-only commit — full gate table + residual risk in the changelog session log. Lint passed; tests not applicable (no code changed). |
| 4 Prep | pending | | | **Blocked on two things:** spec PR #572 approval, and a **Product decision on surface scope** (LD-012, reopened at review round 2). Round 1 (5 findings) addressed in `975ea3f5`; round 2 (5 findings) in `f375f4d6`; round 3 (3 self-contradictions) in `ce0cb91e`; round 4 (2 test-plan defects) in `d496e353` — reviewer now approves the design pending the scope decision. **Paused at user instruction.** |
| 5 Implement | pending | | | |
| 6 Manual review | pending | | | |

Resume: Phase 4 — Plan mode
