# Orchestration — atlas/PRDV-16192

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | `docs/atlas/PRDV-16192/PRDV-16192-original-ticket.md`, `docs/atlas/PRDV-16192/orchestration.md`, `docs/atlas/PRDV-16192-changelog.md` | 2026-07-27 | Ticket captured from the active ClickUp page in a prior session as a loose file at `docs/atlas/PRDV-16192-original-ticket.md`; this session relocated it into the canonical ticket folder via `git mv` (Original Request untouched) and completed the artifact's Explicit Constraints / Context Paths / Capture Gaps sections. Folder convention follows the atlas precedent `docs/atlas/PRDV-XXXXX/` (see PRDV-14055), not `docs/atlas/tickets/<slug>/`. **Capture gap recorded:** the `## Dev Note:` heading has no captured body, yet the QA Notes say the testable behaviour "comes entirely from the dev notes" — resolve in Phase 1 (this ticket vs PRDV-15840). **Repo scope open:** ticket titled "Europa permission change improvements" under ClickUp project "Atlas Maintenance"; emitting vs displaying repo (callisto / europa / atlas-front-end) to be determined in Phase 1. No implementation-repo file touched. |
| 1 Investigate | done | approved investigation plan (`~/.claude/plans/validated-herding-petal.md`) | 2026-07-27 | **Reclassification (headline).** Assumed class = three UI display defects; confirmed class = a contract/shape mismatch — a file-oriented audit schema (`resourcePath`/`path`/`bucket`, one resource per event) carrying a permissions-diff domain, read by a projection written for single-resource file events. Root causes traced by direct read: `europa .../search-audit-events-paginated.transaction.script.ts:55` (`auditEventResources?.[0]` → N−1 changed keys silently dropped) and `:58-62` (`??` chain, so `''` beats the `oldState.path`/`resourcePath` fallbacks). Key consequence: the resource key **is already stored** in `resourcePath` on every historical event, so a read-side fix is retroactive with no backfill. Neighbor of the same class already broken in prod: `MERGED` events are multi-resource (`case-merge-to-audit-event.assembler.ts:24`). Consult: no prior coverage ledger touches the permissions/audit/Europa subsystem (two grep hits were browser-extension `host_permissions`, unrelated). 6 open variables (OV-1…OV-6) carried to Phase 3; 7 code-discoverable facts resolved in-phase, incl. closing the Phase 0 `Dev Note:` capture gap (quoted behavior matches shipped code exactly). |
| 2 Report | done | `investigations/PRDV-16192-investigation.md`, `investigations/PRDV-16192-coverage-ledger.md`, `investigations/PRDV-16192-diagrams.md`, `testing/PRDV-16192-test-plan.md`, `PRDV-16192-why-these-changes.md`, `PRDV-16192-future-development-concerns.md`, `PRDV-16192-pr-draft.md` (shell) | 2026-07-27 | Verdict **proceed with conditions**. Report links (not embeds) the diagrams; §2 Problem Check filled with quote-grounded findings (Conflation and Off both fired). Six open variables carried to Phase 3; six concerns recorded, two of them (silent fire-and-forget audit loss; concurrent-save diff race) newly discovered end-to-end reviews of the permissions audit path, both grounded at `permissions-matrix.service.ts:27-53`. One assumption remains **confirmed directionally** — "`resourcePath` is populated on every historical event" is proven in code but not against live Mongo data; it sits on the coverage-ledger frontier. **Branch caveat from Phase 1 closed.** At user instruction, all three repos moved to `main` and pulled: atlas-front-end `PRDV-14055`→`main` `102e034d` (+20), callisto-back-end `PRDV-16216`→`main` `47f5a841` (+51), europa-back-end already `main` `af49e79`. All three load-bearing findings re-read on `main` and confirmed byte-identical (`search-audit-events-paginated.transaction.script.ts:55,58-62`; `permissions-to-audit-event.assembler.ts:15-24`; `auditEventColumns.ts:43-48`). Coverage-ledger entries are keyed to the `main` SHAs. Pre-existing local edits in callisto (`.swcrc`, `notification-template-preview.html`, untracked `scripts/`) carried across the checkout and were left untouched. **Docs system decision (user, 2026-07-27):** keep artifacts under `docs/atlas/` (ClickUp project = "Atlas Maintenance"); **primary implementation repo is `europa-back-end`** — the root cause and the retroactive fix live in Europa's paginated projection, with Atlas as the render surface and Callisto as the emitter. |
| 3 Probe & spec | in-progress (redirected) | `PRDV-16192-discussion-brief.md` | 2026-07-27 | Auto-advanced from Phase 2. Step 7 reconcile re-run on OV-1…OV-6 and **three sharpened by code evidence**: the legacy `/search` endpoint has **no consumer anywhere** (OV-1 sub-question dissolved); Atlas pagination is genuinely server-side with `row-key="auditEventId"`, and expandable-row precedent exists in four Callisto tables (OV-2 gains a third viable model); `fetchPermissionsMatrix` is imported only by the Permissions Manager page, so the audit page has no label map today (OV-3, on top of the label collision). **USER OVERRIDE (2026-07-27):** grill-me halted before Q1 was answered — "we're not trying to build anything… we are trying to create investigative docs so that we can actually understand the scope of the problem." Agent instructed to **decide the fix-side recommendation itself** and produce an investigation artifact usable as a **source of truth by QA and product** for a discussion the following day. Spec and locked-decision ledger deferred; OV-1…OV-6 become **agenda items for that discussion**, not grill-me questions. Agent decision recorded: read-side (alternative C) as the recommendation, emit-side preserved as a costed alternative. |
| 4 Prep | pending | | | |
| 5 Implement | pending | | | |
| 6 Manual review | pending | | | |

Resume: Phase 3 — Probe & spec (Working mode)
