# Orchestration — atlas/PRDV-16313

Ticket folder: `docs/atlas/PRDV-16313/` (matches the established `docs/atlas/PRDV-XXXXX/` convention used by PRDV-16312, PRDV-16402 and PRDV-16192, not the skill's illustrative `tickets/<slug>/` path).

Implementation repo: `callisto-back-end` (`C:\Users\dustin.thomason\callisto-back-end`) — recorded, not a docs root.

Sibling ticket in the same epic: **PRDV-16312** (`docs/atlas/PRDV-16312/`) — same epic PRDV-15736, same `emit-grant-events` design, same outbox mechanism. Its coverage ledger, locked decisions and concerns are the primary prior-coverage input for Phase 1's consult step.

| Phase | Status | Artifacts | Date | Notes |
| --- | --- | --- | --- | --- |
| 0 Capture | done | `PRDV-16313-original-ticket.md`, `orchestration.md`, `../PRDV-16313-changelog.md`, `stories/PRDV-16313-job-stories-index.md`, `stories/PRDV-16313-job-story-01-client-sees-current-filename.md`, `stories/PRDV-16313-job-story-02-internal-renames-stay-internal.md` | 2026-08-11 | Ledger reconstructed from disk (pre-ledger ticket folder — the original ticket was captured 2026-08-11 before this ledger existed). Two stories drafted from the verbatim request (the request carries one stated motivation plus a distinct boundary outcome in AC3 — split rather than folded). 12 open questions carried, none answered by inference. |
| 1 Recon and plan | done | `investigations/PRDV-16313-recon-and-plan.md` (frozen) | 2026-08-11 | Plan approved. **The ticket's cited wiki spec was read at Phase 1** — the process fix inherited from PRDV-16312, and it paid off: the spec's four acceptance criteria are correct and identical to the ClickUp text, but its Technical Design is wrong on three counts (non-existent emit site, silent on atomicity, silent on the deterministic event id). Two user decisions narrowed scope: the AJSF hole recorded not fixed, and the timestamp generated at the emit site. Together they kept the diff inside one module. |
| 2 Report | done | `investigations/PRDV-16313-investigation.md`, `investigations/PRDV-16313-coverage-ledger.md`, `investigations/PRDV-16313-diagrams.md`, `PRDV-16313-why-these-changes.md`, `PRDV-16313-future-development-concerns.md`, `testing/PRDV-16313-test-plan.md` (seeded), `PRDV-16313-pr-draft.md` (shell), stories 01+02 reconciled + index | 2026-08-11 | Verdict **proceed**, class **not reframed** (missing event on an existing write path). 8 of 15 story questions closed by evidence. Seven concerns recorded (C1–C7). Closed a question two prior ledgers left open: a duplicate deterministic outbox id **silently UPDATEs** rather than raising. |
| 3 Probe & spec | in-progress — **9 of 10 done, held at `P3.spec-submit`** | `specs/PRDV-16313-locked-decisions.md`, `specs/PRDV-16313-spec.md`, `specs/PRDV-16313-spec-comparison.md`, `testing/PRDV-16313-test-plan.md` (refined), stories 01+02 **accepted** + index, concerns C8+C9 | 2026-08-11 | 17 decisions locked (13 by evidence/precedent, 3 grilled, 1 agent call). Both stories accepted, one Dione-owned question carried each. **All six of story 01's criteria rewritten (LD-017); none of story 02's.** `P3.spec-submit` is held for explicit user confirmation — it pushes a branch and opens a PR on the shared `larry-adams` wiki, which is outward-facing. |
| 4 Prep | done | `PRDV-16313-implementation-plan.md` (frozen) | 2026-08-11 | 14 ordered steps, each traced to a decision or criterion. Planned from artifacts, no re-investigation. Names the `P5.spec-approved` waiver and the two shipping assumptions up front. |
| 5 Implement | in-progress — **blocked at `P5.gates`** | `PRDV-16313-implementation-plan.md` (frozen), `testing/PRDV-16313-testing-implementation.md`, test plan results log filled; code on `callisto-back-end` branch `PRDV-16313`, **uncommitted** | 2026-08-11 | Code, specs and docs done: **tsc clean · `test:architecture` PASS (closes A6) · lint PASS · full suite 364 suites / 1889 tests PASS**. Diff is **6 files + 1 new dir, all under `src/granting-client-access/`** — zero in the two neighbour trees. **`npm audit --audit-level=high` exits 1 (42 vulns, 6 high, pre-existing) — a hard stop before commit per `git-commit-workflow`.** So `P5.pr` and `P5.advance` are not done. **`P5.spec-approved` satisfied by WAIVER.** **NP-3b (atomicity) NOT RUN — atomicity is unproven.** |
| 6 Manual review | pending | | | |

Resume: Phase 5 — Working mode, **blocked at `P5.gates` on the audit failure**; `P3.spec-submit` also still held

## Two blockers and one gap, for whoever picks this up

1. **`npm audit --audit-level=high` exits 1 — 6 high advisories. Pre-existing, not introduced here.** Blocks commit per `git-commit-workflow`. **Likely resolvable by rebasing**: the branch sits on stale `main` `71ce3cbf`, and `954f4adb PRDV-16391: npm audit fix` is one of the six commits on `origin/main` `645de730`. The rebase is blocked by a **pre-existing uncommitted `package-lock.json`** (plus `.swcrc`, `notification-template-preview.html`, untracked `scripts/`) that belong to **someone else's session** — deliberately not stashed or discarded while the user was away. **Needs the user's call.**
2. **NP-3b was not executed, so atomicity — the ticket's main added correctness property — is UNPROVEN.** Not inferred as fine: stated as unproven, per the implementation plan's own instruction. The mechanism is verified by reading and the provider is wired correctly, but a plain class provider instead of `createTransactionalProxy` would lose atomicity and **all 1889 tests would still pass**. **Cheapest proof: manual M-5** — `REVOKE INSERT ON callisto.outbox_events`, real rename, confirm `files.file_name` unchanged, `GRANT` back. `callisto-postgres` is already up.
3. **Nothing is committed.** When staging, **never `git add -A`** — stage only this ticket's six paths plus the new assembler directory.

## Ledger notes

- **`P5.spec-approved` — WAIVER, not approval (2026-08-11).** The gate's `done` condition permits proceeding on a recorded waiver naming the authoriser and the risk. Both are here.
  - **Authorised by:** Dustin Thomason, explicitly — *"Can we go ahead and move straight toward implementation? Don't worry about getting approval for that."*
  - **What was waived:** Larry Adams' review of the spec. `specs/PRDV-16313-addendum-draft.md` is **unsubmitted**; `larry-adams` was verified clean on `main` with no `PRDV-16313` branch. `P3.spec-submit` remains held at the user's instruction.
  - **Basis for it being reasonable:** the authoritative spec already exists and was authored by the reviewer; its four acceptance criteria are correct and need no change; the ODP contract and routekey allow-list already ship. Most departures are *forced* (module direction plus two `severity: 'error'` fitness rules) rather than chosen.
  - **Residual risk, itemised — this is what the waiver actually costs:**
    - **LD-019 / concern C10 (the material one).** Deliverability is checked at **request entry only**, not re-established inside the rename/outbox transaction, while the `CLIENT_DELIVERABLE` tag is mutable both ways. If Larry requires transaction-time enforcement: a fresh in-transaction read, row locking, and a new decision about what happens when the answer flips mid-request. **Not a one-line change.**
    - **LD-018.** A successful **no-op** rename emits nothing, which does not satisfy AC1's literal text. If Larry rules every successful `PATCH` must emit, the guard inverts — small code change, plus flipping test EC-1 and manual step M-3's expectation.
    - **LD-009.** The validator returns its loaded context, deviating from ~17 sibling validators that return `void`. If rejected, fall back to widening `RenameProceedingFileProjection` — which touches `src/proceedings` and widens the AJSF response body.
    - **LD-003.** The emitting class is an assembler rather than a transaction script. Forced by `transaction-scripts-no-aggregators`, so a reviewer preference here would need a fitness-rule change, not a code change.
  - **Compounding:** **PRDV-16312 is itself still gated on Larry's `larry-adams` PR #34.** If that response changes payload conventions, decisions here are affected too.
  - **If Larry's response contradicts any of these,** the code changes and the corresponding `LD-###` row is superseded on the record with a why-log course change — not edited silently.
- **Branch base hazard, recorded because it recurred (2026-08-11).** `git checkout main && git pull --ff-only` printed `Updating 71ce3cbf..645de730` then **`Aborting`** — blocked by a pre-existing uncommitted `package-lock.json`. So branch `PRDV-16313` was cut from **stale `main` `71ce3cbf`**, six commits behind `origin/main` `645de730`. **This is the same failure, from the same file, that PRDV-16312's ledger already recorded** — evidently a standing condition of this working copy, not a one-off.
  - **Four items in the working tree are NOT this run's and were deliberately left untouched:** modified `.swcrc`, `notification-template-preview.html`, `package-lock.json`, and untracked `scripts/`. **Never `git add -A` on this branch** — stage only this ticket's paths explicitly.
  - **Assessed impact: none on the code, one on the gates.** None of the six missing commits touch `granting-client-access`, the rename path, the outbox, or the transaction machinery (verified by `git diff --name-only 71ce3cbf..645de730` filtered on those paths). **But `954f4adb PRDV-16391: npm audit fix` is among them**, and PRDV-16312 was **blocked at `npm audit --audit-level=high`** by three high advisories. So the audit gate may fail on this stale base and pass on current `main`.
  - **Required before `P5.gates`:** rebase onto `origin/main`. Deferred rather than forced now, because resolving it means stashing or committing someone else's uncommitted work while they are away.
- **Plan-step wording that reality corrected:** the implementation plan's step 9 says to add the new converter "beside `FileCreatedToOutboxDataConverter`" in the registry. **That converter is not on `origin/main`** — PRDV-16312's six commits are unmerged, pushed only to `origin/PRDV-16312`. Branching from `main` was still correct (it carries PRDV-16293's port, writer and full seven-contract allow-list, which is all this ticket depends on), and stacking on an unmerged branch still under review would have been worse. Consequence: the registry adjacency is cosmetic and the sibling provider is a pattern held in context rather than a file on disk. **Merge-order coupling:** whichever of PRDV-16312 / PRDV-16313 merges second resolves the registry file.

- **Phase 1 / 2 headline (2026-08-11): the spec's acceptance criteria are right; its Technical Design is wrong three times over.** Recorded here because it is what a resuming agent most needs to know. (1) *"Inject `CLIENT_ACCESS_OUTBOX` into the rename transaction script"* — no such script exists in `granting-client-access`; the only rename TS lives in `proceedings`, is shared by three HTTP surfaces, has no `AuthUser`, and would be a module cycle. **And the obvious adaptation is also illegal**: `transaction-scripts-no-aggregators` at `severity: 'error'` forbids a new TS from importing `ProceedingAggregator`, and `services-no-converters` forbids the service holding the payload converter. The one legal shape is an **assembler**. (2) Silent on **atomicity** — the rename path has no transaction, so a naive emit leaves "renamed, no event, forever, silently." (3) Silent on the **deterministic event id**, whose collision mode is a silent row overwrite.
- **A subagent's claim inverted a competing plan's recommendation, and was verified against source before being relied on.** Two Plan agents disagreed on the emit site; the deciding evidence was the fitness rules, which one agent cited and the other did not know about. All three load-bearing claims (`transaction-scripts-no-aggregators`, `services-no-converters`, `repo.save()` on a non-generated uuid PK) were read directly from source rather than taken on report. Worth repeating as a habit: the cheaper failure here would have been to accept the more confident plan.
- **Closed a question two prior coverage ledgers left open.** PRDV-16402 flagged `orbital-relay-pkg` duplicate-id semantics as unresolved; PRDV-16312 reopened it and also left it open. Resolved this pass: `OutboxEvent.id` is a non-generated uuid PK and `OutboxEventRepository.create` ends in `repo.save()`, so a duplicate id **UPDATEs** the prior row — resetting `status`→`PENDING` and `attempts`→`0` — with no exception and no log. **Confirmed directionally only** (read from library + TypeORM source, not observed); report assumption A3 owes a real-Postgres demonstration before it is asserted as fact in the PR.
- **Reopened and corrected a stale sibling finding.** PRDV-16312's ledger records `callisto-back-end/node_modules` as emptied by a failed `npm ci` and not restored, which blocked all of its Phase 5 gates. **Verified populated (823 entries) on 2026-08-11**, with `@planetdepos/orbital-docking-protocol@1.0.7` readable on disk. Re-check before `P5.gates` rather than trusting either note.
- **Two user decisions this run, both narrowing scope — and together they are what makes neighbour protection provable rather than argued.** The AJSF rename hole is recorded as concern C1, not fixed, on the workflow ruling that *"once it is declared a client deliverable, there would be no reason for a user to ever change the name."* And the payload/event-id timestamp is generated at the emit site rather than threaded out of `updateFileName`, on minimal-blast-radius grounds. Result: **zero files touched under `src/proceedings/**` or `src/proceeding-job-submission/**`**, so the shared rename transaction script keeps its four dependencies and no outbox port and is structurally incapable of emitting.
- **Two acceptance criteria are knowingly not met, and neither was reworded to fit.** Job story 01 criterion 1 (client sees the new name) is false via the AJSF route — concern C1. Story 01 criteria 1–4 are written as client-observable and this ticket's boundary ends at an `outbox_events` row, because RabbitMQ is descoped epic-wide. Both carried to Phase 3 as open variables. **Do not silently narrow either one** — rewrite on the record, or supersede the story.
- **2026-08-11 — the comparison was reviewed by the user and CORRECTED before submission. Six findings raised; all six upheld, two of them factual errors in the agent's own analysis.** This is the most important note on the ticket, because revision 1 would have gone to the spec's author carrying two wrong claims.
  1. **`proceedingId` was wrongly reported as unavailable at Larry's named site.** It *is* available — `RenameProceedingFileTS` reads it from `fetchProceedingFileForRename` (this ledger's own coverage area 2 says so). It needs plumbing only because **our** assembler design emits outside that script. **A consequence of our own boundary choice was filed as a defect in his spec.** The genuinely absent value there is authenticated-user identity.
  2. **The chronology was self-contradictory.** The comparison's prose noted `4d284978` *predates* the spec, while its weighting table said the code moved "underneath a maintained document." Corrected: **unchecked assumption at authoring time, not a post-authoring restructure.** Also "the named site does not exist" → **"the assumed *dedicated* boundary does not exist"**; a rename transaction script does exist, it is shared and in another module.
  3. **"All four ACs are met literally" was false.** AC1 covers a successful `PATCH`; a successful **no-op** writes nothing. Now **question 1** for Larry, with suggested wording — see LD-018.
  4. **The tag decision was framed too narrowly.** Re-reading the same `isDeliverable` value is dead code, but the validator runs **outside** the new transaction and the tag is **mutable both ways** — so a *fresh in-transaction* check is a different guarantee. Now **question 2** for Larry — see LD-019 and concern C10. The earlier "do you want a dead branch?" was the wrong question.
  5. **NP-3 could not prove what it claimed.** Rollback was assigned to a unit suite with aggregator and outbox both mocked. **Split into NP-3a (unit: ordering + propagation) and NP-3b (real Postgres: actual rollback)**, plus manual step M-5 fault injection. Atomicity is the main property added beyond the spec, so it cannot rest on mocks. **If NP-3b cannot run, atomicity is reported unproven.**
  6. **Two overclaims softened.** A non-transactional outbox failure is **not silent** — the error propagates and the caller gets a 500; the defect is that the filename commits while the response reports failure. And A3 is **not "closed"** — source-inspected, unobserved, EC-5 blocked; now stated as an evidence-backed hypothesis and residual.
- **`specs/PRDV-16313-addendum-draft.md` (2026-08-11) — DRAFT, not submitted.** Written from the user's dictated input as an **implementation and risk addendum**, explicitly not a competing specification. Larry's spec remains authoritative for behaviour. Leads with what the spec got right, asks **two** questions, and frames the rest as implementation constraints, additive correctness, or accepted risks rather than blockers. **Nothing has been pushed to `larry-adams`. Do not push or submit without Dustin's explicit authorization.**
- **`specs/PRDV-16313-spec-comparison.md` (2026-08-11) — written at the user's direction, and it changed the submission's framing rather than its content.** *(Now at revision 2 — see the correction note above; revision 1's weighting and two of its facts were wrong.)* The user asked for a direct assessment of Larry's spec *after* ours existed, instead of going straight to the addendum PR. Verdict: **the spec's contract-level commitments are all correct** (payload correct field-for-field against shipped ODP 1.0.7, routekey correct, semantics useful, all four ACs correct and consistent with ClickUp, testing bullets maintained through the RabbitMQ descope) — **no reason to go against any of them.** Its *Technical Design* breaks for four distinguishable reasons, and separating them is the point: **2 stale** (commit `4d284978` restructured the rename path under a maintained document), **2 unchecked assumptions** (the specified payload cannot be built at the specified site — `proceedingId` is absent from the only projection the rename returns), **2 foreseeable omissions** (atomicity, the no-op branch), **1 not fairly a miss** (the deterministic-id overwrite, unreachable until this ticket and left open by two prior investigations). **One item is Larry's decision, not ours** — the tag-guard mechanism (D3). Everything else is forced or additive.
- **Phase 2 gate evidence (2026-08-11).** `check-steps.ps1 -TicketFolder docs/atlas/PRDV-16313 -ThroughPhase 2` → `n/a=6 REVIEW=7 VERIFIED=10`. **All nine of Phase 2's file-producing steps VERIFIED** (`P2.plan-saved`, `P2.why`, `P2.story-reconcile`, `P2.report`, `P2.coverage`, `P2.diagrams`, `P2.concerns`, `P2.test-plan`, `P2.pr-shell`); `P2.session-log` and `P2.advance` are `n/a` to the script by design. The seven `REVIEW` rows are all Phase 0/1 human-judgment items, each satisfied: `P1.consult` (the ledger's Consulted section ran before any investigative branch opened, and records one substantive reuse plus one reopen), `P1.stage-story` (8 of 15 questions closed, with the two overreaching criteria recorded rather than reworded), `P1.stage-coverage` (consult line first, then eleven area rows, then the frontier), `P1.stage-why` (problem class stated, Phase 1 why-log entry with obvious / not-obvious / assumptions / noise), `P1.plan` (carries findings, not just todos — re-derivable after a context loss), plus the two Phase 0 rows re-confirmed.
- **No `callisto-back-end` file created, edited, or run in Phase 2.** The entry check permits it now that Phases 0 and 1 read `done`, but no implementation work has begun and no branch has been cut. Everything so far is docs in `dustin-thomason`.

- **2026-08-11 — reconstruction (resume protocol case 3).** Invoked against an existing folder holding only `PRDV-16313-original-ticket.md`. State was reconstructed from disk: `P0.ticket` was already satisfied; `P0.ledger`, `P0.changelog`, `P0.stories` and `P0.advance` were open and are now closed.
- **`PRDV-16313-original-ticket.md` carries a `## Downstream Artifacts` section that the current `original-ticket-artifact.md` no longer specifies** (it was removed from the template because it duplicated this ledger's Artifacts column). The artifact is immutable once captured, so it is **not** being edited to conform. The authoritative record of what this ticket produces is the Artifacts column above.
- **The ticket's `## Wiki` path is stale, and this is already a known defect.** The ticket names `systems/neptune/callisto/granting-client-acess/emit-grant-events/PRDV-16313-endpoint-file-renamed.md`. That folder was renamed; the file actually lives at `systems/neptune/callisto/granting-client-acess/epic-PRDV-15736-make-atlas-metadata-available-to-planet-portal/PRDV-16313-endpoint-file-renamed.md` (verified via `git ls-files` in `larry-adams` on `main`). PRDV-16312's run found the same rename and recorded it as a vault defect (10 broken wiki-links from `systems/README.md`) — deliberately not fixed there. Resolved path recorded here so Phase 1 does not repeat the lookup.
- **Carried-forward process fix from PRDV-16312: `larry-adams` is a Phase 1 input, not a Phase 2 discovery.** That run read the ticket's cited wiki spec only at Phase 2, after the user asked, and reached a wrong disposition on half the ticket as a result. For this ticket the wiki spec **and** `dione-file-access-event-design.md` are mandatory reads inside `P1.consult`.
- **`larry-adams` is not read-only for specs.** PRDV-16312 corrected an over-broad claim in its own artifacts: the repo takes squash-merged spec PRs and has a real review cycle. The narrow restriction is changelog/workflow bookkeeping files. Spec submission at `P3.spec-submit` therefore goes through a branch + PR there, per precedent PR #34.
- **Known epic-level scope change to carry in: RabbitMQ was descoped by Derrick** (PRDV-16312 LD-007; upstream commit `318bd0a` "PRDV-15736: remove rabbitMQ work from scope of epic" applied it across nine specs). Any dev-queue / "message visible in RabbitMQ" acceptance criterion is out of scope epic-wide. The producer's obligation ends at a correctly shaped `outbox_events` row. Confirm at Phase 1 that this ticket's wiki spec carries the applied change.
- **Open risk inherited from the sibling, not this ticket's to resolve:** PRDV-16312 shipped under a **waiver** on `P5.spec-approved` and is still gated on Larry's response to `larry-adams` PR #34, with concern C7 (`deliverableCollectionValue` overwriting seeded static collections) unverified. If that response changes the payload conventions, this ticket's payload work is affected. Check PRDV-16312's ledger state before Phase 3 locks decisions here.
- **PRDV-16312 left `callisto-back-end/node_modules` emptied and unrestored** (`npm ci` cleared it, then failed E401), which blocked all of its Phase 5 gates. Verify and resolve before this ticket reaches `P5.gates` — do not discover it at the gate.
- **No `docs/callisto/` exists** in this repo; all PlanetDepos ticket memory lives under `docs/atlas/`, including backend-only tickets (see PRDV-16216 and PRDV-16312). Changelog therefore goes to `docs/atlas/PRDV-16313-changelog.md`.
- **Phase 0 gate evidence (2026-08-11).** `check-steps.ps1 -TicketFolder docs/atlas/PRDV-16313 -ThroughPhase 0` → `n/a=2 REVIEW=2 VERIFIED=1`.
  - `P0.ledger` **VERIFIED**.
  - `P0.ticket` **REVIEW** → satisfied on judgment: the Original Request section is the ClickUp description verbatim from a browser DOM capture, not a paraphrase.
  - `P0.stories` **REVIEW** → satisfied on judgment: both stories are `draft`, both have index rows written in the same pass, and no acceptance criterion in either story names a design element (checked each of the 11 criteria — no endpoint, routekey, payload type, tag name, or transport survives into the criteria; the request's four mechanism criteria are recorded as *input* in the index instead).
  - `P0.changelog` and `P0.advance` are `n/a` to the script by design (the changelog lives outside the ticket folder; advance is an actor).
- **No `callisto-back-end` file was created, edited, or run during Phase 0** — the entry check in `SKILL.md` forbids touching the implementation repo until Phases 0 and 1 both read `done`.
