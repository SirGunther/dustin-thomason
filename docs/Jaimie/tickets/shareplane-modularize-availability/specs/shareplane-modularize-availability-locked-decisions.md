# Locked decisions — Jaimie/shareplane-modularize-availability

Traceability ledger (per qa-to-spec-traceability). Each row: the decision, its source/evidence, what it supersedes or rejects, and where it lands in the spec. Risk-accepting rows cite a concern entry.

| ID | Decision | Source / evidence | Supersedes / rejects | Spec destination | Status |
| --- | --- | --- | --- | --- | --- |
| LD-001 | **Delivery model = MV3 browser extension, presented as a full-page dashboard tab** (icon click → `chrome.tabs.create(dashboard.html)`; extension-origin page authorized via `host_permissions`). Not a floating popup; not a local file; not a SharePoint-hosted page. | Spike: F1 same-origin `_api` read CONFIRMED, F4 file:// CORS block CONFIRMED (report §13). User: "An extension works… I want to use it in an actual page. I want the screen real estate." OtterCopy MV3 precedent. | Rejects: local file (F4 dead), MSAL/OAuth, SharePoint-hosted/SPFx page (fallback only), floating popup. | Spec §Delivery / architecture | locked |
| LD-002 | **F2 (browser attaches the session cookie to the extension-origin `_api` fetch) is accepted as strongly-supported and proven at build time in Phase 5**, not via a pre-build spike. | User direction: "I trust it should work… we need to build it all in and do it correctly, and we'll see if it works." Grounded in F1 (cookie read proven same-origin) + established MV3 host_permissions cookie behavior. Stage-2 pre-build test was inconclusive (extension did not load — flag no-op'd) and deliberately not pursued per browser-loop guardrail #6. | Rejects: gating the whole build on a standalone F2 spike. | Spec §Delivery; test plan F2 → Phase 5 execution | locked (risk-accepted → concern FDC-5) |
| LD-003 | **"Availability" = per-list count** ("OJB: 1 · Archive: 2 · VUL: 0"), not yes/no and not a status field. A true count uses `$inlinecount=allpages` (or a sufficient `$top`), since the record can appear multiple times per list and across lists. | Grill-me D1. Spike §13/§1b evidence: 627259 ×5 in Archive, 621571 ×2. | Rejects: existence-only yes/no; status-field variant. | Spec §Feature behavior | locked |
| LD-004 | **Name = "SharePoint Shareplane"** (adopt the tool's own branding). Rename the file/extension away from "SharePoint Lookup". | Grill-me D3. Internal `<title>`/H1 already say "Shareplane". | Rejects: "SharePoint Lookup"; new name. | Spec §Naming; manifest name | locked |
| LD-005 | **Trigger: single mode auto-checks (debounced ~400ms); bulk mode checks on an explicit "Check availability" button.** Per-list count badges render inline. | Grill-me D4. | Rejects: always-on-demand; always-auto (throttle risk on bulk). | Spec §Feature behavior / UX | locked |
| LD-006 | **Bulk: batch titles into per-list `$filter` queries (not one call per title), cap ~30 titles per check, show a visible "split into smaller batches" warning past the cap.** Mirrors the existing URL-length warning; prevents silent partial results. | Grill-me D5. Prior coverage: 5,000-item threshold, ~300 calls/60s, OData OR-clause limits. | Rejects: unbounded sequential; one-call-per-title. | Spec §Feature behavior / bulk | locked |

## All grill-me decisions resolved (D1–D5 + delivery/F2). No open decisions remain for this phase.
