# Cairn Capability Catalog

This catalog describes Cairn's UI as frozen in `Cairn-POC-v1-2026-08-20` and records the
Phase 0 architecture proofs beside it. It separates what actually works from what is
simulated, so a good-looking prototype is not mistaken for a working tool.

**The single most important line in this document:** nothing in Cairn writes to disk. Every
save target is browser storage or memory.

## Status legend

- **Working** — implemented and verified against a real check.
- **UI POC** — implemented in the browser, but the behavior is simulated, in-memory, or
  scoped to the embedded sample vault.
- **Specified** — documented or styled, but not connected end to end.
- **Deferred** — deliberately reserved for a later slice.

## Vault and file access

| Surface or control              | Status    | Current behavior                                                                                                                                                                                                                                                                                                               | Implementation reference                                              |
| ------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------- |
| Embedded sample vault           | Working   | Seven `.md` files load from a generated JS literal, because `fetch()` is blocked on `file://`.                                                                                                                                                                                                                                 | `sample-vault.js`, `tools/build-sample-vault.mjs`                     |
| Add folder to workspace         | UI POC    | Reads a real Windows directory of `.md` files via `showDirectoryPicker()`, **additively** — each call appends a root. Read-only. Capped at 2000 files across the whole workspace and 12 levels deep; skips dotfiles and `node_modules`, `dist`, `build`, `out`, `coverage`.                                                    | `addRoot()`, `walkDirectory()`                                        |
| Multiple workspace roots        | UI POC    | Each root renders as a collapsed top-level node named for its folder and becomes its own section tab. Same-named roots get a counted suffix, because the picker exposes only `handle.name` and never the parent path. Per-root remove control. The first real root replaces the sample vault; every root after it is additive. | `addRoot()`, `removeRoot()`, `isRootPath()`                           |
| Root persistence across reloads | Deferred  | None. A picked folder is gone on reload; the sample vault returns. Phase 0D adds a gesture-driven probe for a posted `FileSystemDirectoryHandle`; persistence still needs IndexedDB plus a user-gesture permission re-grant.                                                                                                   | `DECISIONS-PENDING.md` → `ADP-002`; `Architecture/Phase0DEvidence.md` |
| Writing to disk                 | Deferred  | **No write path exists.** No `createWritable`, no conflict check, no atomic replace.                                                                                                                                                                                                                                           | `DECISIONS-PENDING.md` → `ADP-001`                                    |
| External change detection       | Deferred  | None, and not achievable in-browser — the File System Access API fires no change events. An edit made in Cursor will not appear.                                                                                                                                                                                               | `DECISIONS-PENDING.md` → `ADP-001`                                    |
| Save                            | UI POC    | `Ctrl` `S` and a 900ms debounce write to `localStorage` under `cairn-poc-v1`. Sample-vault edits persist; folder-mode edits stay in memory only, deliberately, so a stored overlay cannot shadow a file that changed on disk.                                                                                                  | `save()`, `persist()`                                                 |
| Browser support                 | Specified | Built and verified against Chromium. The folder picker is feature-detected and disables itself with an explanatory tooltip elsewhere; the rest is untested outside Chrome and Edge.                                                                                                                                            | `boot()`                                                              |

## Explorer

| Surface or control    | Status   | Current behavior                                                                                                                                                                                    | Implementation reference                                 |
| --------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| File tree             | Working  | Nested folders built from vault paths, folders before files, natural-order sort. The UI POC uses `buildTree()`; the Phase 0 graph now proves the equivalent DOM-free `vault-index` tree projection. | `buildTree()`, `renderTree()`, `components/vault-index/` |
| Expand / collapse     | Working  | Per-folder toggle, persisted across reloads.                                                                                                                                                        | `setExpanded()`                                          |
| Per-folder file count | Working  | Recursive `.md` count on each folder row.                                                                                                                                                           | `countFiles()`                                           |
| Collapse all          | Working  | Clears every expanded folder.                                                                                                                                                                       | `#collapseAllButton`                                     |
| Keyboard navigation   | Working  | `↑` `↓` move, `→` expands, `←` collapses or goes to parent, `Enter` opens, `Space` favorites.                                                                                                       | `onTreeKeydown()`                                        |
| Section tabs          | Working  | Top-level folders become OneNote-style tabs with stable per-position accent colors, plus an "All notes" tab. Selecting a tab promotes that folder's children to the root.                           | `renderSections()`, `visibleNodes()`                     |
| Favorites             | Working  | Starred from the tree or the toolbar, pinned above the explorer, persisted. Hidden entirely when empty.                                                                                             | `toggleFavorite()`, `renderFavorites()`                  |
| Resizable sidebar     | Working  | Pointer drag and arrow keys, clamped 190–460px, persisted.                                                                                                                                          | `bindResizer()`                                          |
| Quick open            | Working  | `Ctrl` `P`, subsequence match over paths, scored for consecutive runs and filename hits, matched characters highlighted. Filenames only — **not** content.                                          | `fuzzyMatch()`, `renderPalette()`                        |
| Full-text search      | Deferred | Not implemented.                                                                                                                                                                                    | `DECISIONS-PENDING.md` → `SRC-001`                       |

## Document

| Surface or control                  | Status    | Current behavior                                                                                                                                                                                                                                | Implementation reference           |
| ----------------------------------- | --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| Markdown rendering                  | Working   | Delegated to `vendor/markdown-renderer.js`, a byte-identical copy of `WorkLists/public/markdownRenderer.js`. Headings, lists, nested lists, task lists, tables with alignment, fenced code, blockquotes, rules, inline formatting, autolinking. | `renderDoc()`                      |
| **Checkbox → source rewrite**       | Working   | Clicking a rendered checkbox splices **exactly one** source line via `updateTaskCheckboxMarkdown()`. Verified: line count unchanged, one line differs. Reverts the box and warns if the source disagrees.                                       | `handleCheckbox()`                 |
| Preview mode                        | Working   | Default state. Centered reading column capped near 68ch.                                                                                                                                                                                        | `setMode()`                        |
| Split mode                          | Working   | Preview and source side by side on one grid row, each half the width.                                                                                                                                                                           | `.doc-body[data-mode="split"]`     |
| Source mode                         | UI POC    | A styled `<textarea>`, `wrap="off"`, with a line-number gutter locked to scroll. No syntax highlighting, folding, or bracket matching.                                                                                                          | `#source`, `renderGutter()`        |
| Mode cycling                        | Working   | `Ctrl` `E` cycles preview → split → source.                                                                                                                                                                                                     | `onGlobalKeydown()`                |
| Inline editing in the rendered view | Deferred  | Not implemented. **Constraint already settled:** never whole-document HTML round-tripping — measured 12-of-41-line loss including every checkbox state.                                                                                         | `DECISIONS-PENDING.md` → `EDN-001` |
| Tab strip                           | Working   | Multiple open documents, dirty dot, close button, middle-click close, `Alt` `1`–`9` jump, `Alt` `W` close.                                                                                                                                      | `renderTabs()`                     |
| Breadcrumb                          | Working   | Full vault-relative path above the document.                                                                                                                                                                                                    | `renderBreadcrumb()`               |
| Status bar                          | Working   | Path, word count, `done/total` task count, view mode, save state.                                                                                                                                                                               | `renderDoc()`                      |
| Code-block copy                     | UI POC    | Copies to clipboard and shows a confirmation. Falls back to a warning toast when the clipboard is blocked, which happens on `file://`.                                                                                                          | `copyCodeBlock()`                  |
| Single newline handling             | Specified | Renders as a hard `<br>`, inherited from the WorkLists renderer. Correct for card notes; an open question for full documents because it changes how every soft-wrapped note reads.                                                              | `DECISIONS-PENDING.md` → `BRK-001` |
| Scroll sync between split panes     | Deferred  | Not implemented.                                                                                                                                                                                                                                | —                                  |

## Outline and navigation

| Surface or control     | Status  | Current behavior                                                                                                                                                                                                                                            | Implementation reference                 |
| ---------------------- | ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------- |
| Outline rail           | Working | Every heading, indented by level, click to jump. Hideable, state persisted.                                                                                                                                                                                 | `renderOutline()`                        |
| Heading pointer        | Working | `Alt` `↓` / `Alt` `↑` step through headings with an accent bar on the current one, and the pointer follows the reader on scroll so the next jump continues from where they are.                                                                             | `goToHeading()`, `applyHeadingPointer()` |
| Heading source mapping | Working | Heading source lines are found by scanning the same text the renderer sees, skipping fenced regions so a `#` inside a code block cannot desynchronize the Nth DOM heading from the Nth scanned line. In source and split modes the pointer moves the caret. | `headingSourceLines()`                   |

## Presentation

| Surface or control       | Status    | Current behavior                                                                                                                                                                                                                                                                                        | Implementation reference                  |
| ------------------------ | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------- |
| Palette                  | Working   | Three complete token blocks — Argus dark (default), light, WorkLists dark. No component rule names a color; verified by scanning `styles.css` for literal colors outside the token blocks. Body prose links use a separate `--link` token so the Argus lime accent never lands inside a reading column. | `styles.css` `:root`, `[data-theme=…]`    |
| Theme switching          | Working   | A topbar control cycles Argus → light → WorkLists, persisted. The theme is stamped on `<html>` **before first paint** by a separate early script, verified by reading `data-theme` at the first `readystatechange`.                                                                                     | `theme.js`, `applyTheme()`                |
| Light theme fidelity     | Specified | Switches correctly and is legible, but code blocks, task checkboxes, and table headers were tuned against dark first, and the accent hue changes between themes rather than carrying across.                                                                                                            | `DECISIONS-PENDING.md` → `PAL-003`        |
| Reduced motion           | Working   | Shortens transitions to 80ms rather than deleting them, matching the conclusion SaySlate 1.10.1 reached.                                                                                                                                                                                                | `@media (prefers-reduced-motion: reduce)` |
| Surface tokens           | Working   | One `--text-surface` token is the single source for editable and monospace surfaces.                                                                                                                                                                                                                    | `styles.css`                              |
| Tooltips                 | Working   | Pure-CSS `[data-tooltip]::after`: two lines (intent, then shortcut), capped at `min(280px, 100vw - 24px)`, edge-aware by scoped override rather than JS measurement, shown on `:focus-visible` as well as hover. Readable on **disabled** controls, which is the main reason it replaced `title=`.      | `styles.css` `[data-tooltip]`             |
| Laptop-height adaptation | Working   | Chrome and padding step down at 820px and 680px viewport heights through `--topbar-h` and `--doc-pad-y`; the shell uses `100dvh` so browser chrome cannot hide the status bar. Action groups wrap rather than clip. Verified: zero overflow and a visible status bar at 768, 680, and 620px tall.       | `@media (max-height: …)`                  |
| Icon-only topbar         | Working   | Quick open, add folder, and theme are icon buttons at the same subdued weight as the rest of the chrome — no raised primary-action treatment at idle. Preview/Split/Source stay text, deliberately: three icons meaning "view mode" are not distinguishable.                                            | `.topbar-actions`                         |
| Horizontal overflow      | Working   | Zero page-level horizontal scroll, verified at 1520px and 960px. Wide content scrolls inside its own container.                                                                                                                                                                                         | `.markdown-table-wrapper`, `.doc-pane`    |
| Toasts                   | Working   | Top-centered, kind-coded by left border, auto-dismissed.                                                                                                                                                                                                                                                | `toast()`                                 |
| Empty state              | Working   | Shown when no document is open.                                                                                                                                                                                                                                                                         | `#emptyState`                             |
| Font stack               | Working   | `"Segoe UI"` for prose, `"Cascadia Mono"` for monospace, matching WorkLists.                                                                                                                                                                                                                            | `styles.css`                              |
| Icon set                 | Working   | Inline stroked SVG, no icon font, so the POC runs offline. WorkLists' Font Awesome copy icon falls back to its text label.                                                                                                                                                                              | `index.html`                              |

## Verification posture

| Aspect                     | Status   | Detail                                                                                                                                                                                                                                                                                                                                                                                                                    |
| -------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| Automated regression suite | Deferred | Phase 0 has a zero-dependency `package.json`: `npm test` guards the 23 graph-preparation checks. Three browser suites guard 17 kernel checks, 12 standalone component checks, and the worker-count measurement, but **none of them is wired into `npm test`** — each must be invoked by name, so a `npm test` pass does not mean the browser checks ran. The POC UI invariants remain unguarded, so `TST-001` stays open. | `DECISIONS-PENDING.md` → `TST-001`; `Architecture/Phase0DEvidence.md`                  |
| Ad-hoc verification        | Working  | Playwright is resolved from the WorkLists workspace's `node_modules`. Phase 0D adds component-port, tree, handle-transfer, and worker-count evidence without changing the frozen UI files.                                                                                                                                                                                                                                | `tests/component-standalone.mjs`, `Architecture/probes/directory-handle-transfer.html` |
| Formatting gate            | Working  | `prettier --check .` using the WorkLists binary and config. `vendor/` and the generated `sample-vault.js` are ignored so parity diffs stay readable.                                                                                                                                                                                                                                                                      |
| Lint / audit gates         | N/A      | No lint or audit script is declared; no benchmark dependency was added.                                                                                                                                                                                                                                                                                                                                                   |

## Known limitations

- Nothing writes to disk, in any mode.
- A picked folder does not survive a reload; every root must be re-picked each session.
- External edits made in another editor are invisible.
- Only filenames are searchable, not content.
- Light mode works but was derived from a neighbouring project rather than designed; see `PAL-003`.
- Chromium only; Firefox has no directory picker.
- No screen-reader pass. Roles and labels are present, but the tree uses a
  single-tabstop-plus-arrow model rather than a full ARIA treegrid.
- The POC's checkbox-splice and split-pane geometry invariants remain outside the committed
  Phase 0D harness; this is the remaining `TST-001` gap.
