# dnu — retired, kept for posterity

Files here are superseded, not deleted. Nothing references them and nothing should.
Kept because the reasoning in them may still be useful, and because throwing work
away loses the trail.

This folder sits outside `docs/` and `skills/` on purpose, so `sync-rules.ps1` does
not mirror it into `.cursor/` or `.claude/`.

| File | Retired | Superseded by |
| --- | --- | --- |
| `ticket-orchestration.md` | 2026-08-04 | `skills/orchestrate/SKILL.md` — this was only ever a pointer saying the phase sheet had moved there |
| `orchestration-sequence.md` | 2026-08-04 | `skills/orchestrate/steps.csv` plus `render-sequence.ps1`. Six of its statements were superseded before retirement — three on appending Downstream Artifacts to the original ticket, three on a post-approval test-plan revision. See `decisions.md` 35. |
